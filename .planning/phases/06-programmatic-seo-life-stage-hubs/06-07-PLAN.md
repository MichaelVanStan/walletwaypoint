---
phase: 06-programmatic-seo-life-stage-hubs
plan: 07
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - content/cities/new-york-city.yaml
  - content/cities/los-angeles.yaml
  - content/cities/chicago.yaml
  - content/cities/houston.yaml
  - content/cities/phoenix.yaml
  - content/cities/philadelphia.yaml
  - content/cities/san-antonio.yaml
  - content/cities/san-diego.yaml
  - content/cities/dallas.yaml
  - content/cities/san-jose.yaml
  - content/cities/austin.yaml
  - content/cities/jacksonville.yaml
  - content/cities/fort-worth.yaml
  - content/cities/columbus.yaml
  - content/cities/charlotte.yaml
  - content/cities/indianapolis.yaml
  - content/cities/san-francisco.yaml
  - content/cities/seattle.yaml
  - content/cities/denver.yaml
  - content/cities/nashville.yaml
  - content/cities/oklahoma-city.yaml
  - content/cities/portland.yaml
  - content/cities/las-vegas.yaml
  - content/cities/memphis.yaml
  - content/cities/miami.yaml
  - src/app/calculators/rent-affordability/[city]/page.tsx
autonomous: true
requirements: [PSEO-07, PSEO-08]

must_haves:
  truths:
    - "User can access city-specific rent affordability calculators for 25 metro areas"
    - "Each city rent page includes city-specific median rent data and local cost context"
  artifacts:
    - path: "content/cities/new-york-city.yaml"
      provides: "NYC rent data with median rents by bedroom count"
      contains: "cityName: New York City"
    - path: "src/app/calculators/rent-affordability/[city]/page.tsx"
      provides: "25 city-specific rent calculator pages"
      exports: ["generateStaticParams", "generateMetadata", "default"]
  key_links:
    - from: "src/app/calculators/rent-affordability/[city]/page.tsx"
      to: "#site/content"
      via: "cities Velite collection"
      pattern: "import.*cities.*from.*#site/content"
---

<objective>
Create 25 city rent YAML data files and city-specific rent affordability calculator pages at /calculators/rent-affordability/[city].

Purpose: City-specific rent calculators (PSEO-07, PSEO-08) target "rent affordability [city]" search queries with pre-populated local median rent data, making the calculator immediately useful for each metro area.

Output: 25 city YAML files with HUD FMR rent data and 25 city-specific rent calculator pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-programmatic-seo-life-stage-hubs/06-CONTEXT.md
@.planning/phases/06-programmatic-seo-life-stage-hubs/06-RESEARCH.md
@.planning/phases/06-programmatic-seo-life-stage-hubs/06-UI-SPEC.md
@.planning/phases/06-programmatic-seo-life-stage-hubs/06-02-SUMMARY.md

<interfaces>
From #site/content (Velite, after Plan 01):
```typescript
export const cities: City[];
// City has: slug, cityName, stateName, stateAbbreviation, medianRents (studio/1/2/3/4 bed),
// dataSource, dataYear, lastVerified, editorialTitle, editorialDescription, editorialContent,
// faqs, costContext
```

From src/app/calculators/[slug]/page.tsx:
```typescript
// Existing rent-affordability calculator at /calculators/rent-affordability
// Uses calculateRentAffordability from math module and rentParams from url-params
```

From src/lib/calculators/math/rent-affordability.ts:
```typescript
export function calculateRentAffordability(params: Record<string, number | string>): CalculatorResults;
// Takes: income (monthly), maxPct, utilities, other
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 25 city rent YAML data files</name>
  <files>
    content/cities/*.yaml (25 files)
  </files>
  <read_first>
    velite.config.ts,
    .planning/phases/06-programmatic-seo-life-stage-hubs/06-RESEARCH.md
  </read_first>
  <action>
Create 25 YAML files in `content/cities/` for the top 25 metro areas (per 06-RESEARCH.md recommended cities list).

Each file follows the Velite City schema. Data sourced from HUD Fair Market Rents FY2026.

**Cities (from 06-RESEARCH.md):**
1. new-york-city.yaml (NY)
2. los-angeles.yaml (CA)
3. chicago.yaml (IL)
4. houston.yaml (TX)
5. phoenix.yaml (AZ)
6. philadelphia.yaml (PA)
7. san-antonio.yaml (TX)
8. san-diego.yaml (CA)
9. dallas.yaml (TX)
10. san-jose.yaml (CA)
11. austin.yaml (TX)
12. jacksonville.yaml (FL)
13. fort-worth.yaml (TX)
14. columbus.yaml (OH)
15. charlotte.yaml (NC)
16. indianapolis.yaml (IN)
17. san-francisco.yaml (CA)
18. seattle.yaml (WA)
19. denver.yaml (CO)
20. nashville.yaml (TN)
21. oklahoma-city.yaml (OK)
22. portland.yaml (OR)
23. las-vegas.yaml (NV)
24. memphis.yaml (TN)
25. miami.yaml (FL)

Each YAML file must include:
- `slug`: lowercase-hyphenated city name
- `cityName`: Full city name
- `stateName`: State name
- `stateAbbreviation`: Two-letter code
- `medianRents`: Object with studio, oneBed, twoBed, threeBed, fourBed median monthly rents in dollars. Use HUD FMR FY2026 data. Example for NYC: studio ~$1,800, oneBed ~$2,050, twoBed ~$2,450, threeBed ~$3,100, fourBed ~$3,400 (approximate FMR-based).
- `dataSource`: "HUD Fair Market Rents FY2026"
- `dataYear`: 2026
- `lastVerified`: "2026-03-30"
- `editorialTitle`: "Rent Affordability Calculator for [City], [State Abbreviation] 2026"
- `editorialDescription`: Under 300 chars, unique per city (e.g., "Calculate how much rent you can afford in New York City based on your income, with local median rent data for studios through 4-bedroom apartments.")
- `editorialContent`: 200+ words of city-specific rent context: local housing market conditions, neighborhood rent variation, cost-of-living factors unique to this city, tips for renters
- `faqs`: 3-5 city-specific rental FAQs (e.g., "What is the average rent in [City]?", "Is [City] affordable for renters?", "What percentage of income should go to rent in [City]?")
- `costContext`: 1-2 paragraph local affordability context (e.g., "New York City is one of the most expensive rental markets in the US, with median rents significantly above the national average...")

**Quality bar:** Each city must have genuinely unique content about local rental conditions. Do not reuse the same content template with city names swapped.

Run `npx velite build` after creating all files to validate against schema.
  </action>
  <verify>
    <automated>npx velite build 2>&1 | tail -5 && ls content/cities/*.yaml | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - content/cities/ contains exactly 25 .yaml files
    - content/cities/new-york-city.yaml contains `cityName: New York City`
    - content/cities/new-york-city.yaml contains `medianRents:` with studio/oneBed/twoBed/threeBed/fourBed
    - content/cities/new-york-city.yaml contains `dataSource: "HUD Fair Market Rents FY2026"`
    - All 25 files contain `editorialContent:` field
    - All 25 files contain `faqs:` array
    - `npx velite build` exits 0
  </acceptance_criteria>
  <done>25 city rent YAML files created with HUD FMR data, validated by Velite</done>
</task>

<task type="auto">
  <name>Task 2: City-specific rent calculator pages at /calculators/rent-affordability/[city]</name>
  <files>
    src/app/calculators/rent-affordability/[city]/page.tsx
  </files>
  <read_first>
    src/app/calculators/[slug]/page.tsx,
    src/app/calculators/paycheck/[state]/page.tsx,
    src/lib/calculators/math/rent-affordability.ts,
    src/components/calculator/calculator-page-client.tsx,
    src/components/seo/faq-schema.tsx,
    src/components/ads/ad-break.tsx,
    src/lib/metadata.ts,
    content/calculators/rent-affordability.yaml,
    .planning/phases/06-programmatic-seo-life-stage-hubs/06-UI-SPEC.md
  </read_first>
  <action>
Create `src/app/calculators/rent-affordability/[city]/page.tsx` — 25 city-specific rent calculator pages (PSEO-07, PSEO-08).

Per UI-SPEC City Rent Calculators section: "Standard calculator layout, pre-populated with city median rents. Below calculator: city-specific cost context."

**Page structure:**
```
[Breadcrumb: Home > Calculators > Rent Affordability > {City Name}]
[DisclaimerBanner]
[H1: "Rent Affordability Calculator for {City Name}, {State Abbreviation} 2026"]
[Subtitle from YAML editorialDescription]
[Calculator: same rent-affordability calculator but with income default adjusted for city]
[AdBreak]
[H2: "Median Rents in {City Name}"]
[Table showing median rent by bedroom count: Studio, 1-Bed, 2-Bed, 3-Bed, 4-Bed]
[H2: "Renting in {City Name}"]
[Editorial content from YAML]
[H2: "Local Affordability Context"]
[costContext from YAML]
[AdBreak]
[H2: "Frequently Asked Questions"]
[FAQ accordion]
[FaqSchema JSON-LD]
[H2: "Explore Other Cities"]
[City link grid: 6-8 nearby or popular cities]
[Financial disclaimer]
```

**Implementation details:**

1. **generateStaticParams:** Return all 25 city slugs from Velite `cities` collection.

2. **generateMetadata:** Unique title/description per city from YAML.

3. **ISR:** Export `const revalidate = 86400`.

4. **Calculator:** Render the existing rent-affordability calculator (via CalculatorPageClient) but with defaults adjusted. The simplest approach: create an inline config that mirrors `content/calculators/rent-affordability.yaml` but overrides the income default based on city median rent (e.g., for NYC, set default income higher). OR simply use the standard calculator config and let users adjust.

5. **Median rent table:** A simple shadcn Table showing the 5 bedroom counts and their median monthly rent in currency format.

6. **City navigation:** "Explore Other Cities" grid linking to other city rent pages.

7. **Route coexistence:** `/calculators/rent-affordability/[city]` is a nested route under the existing `/calculators/[slug]` pattern which handles "rent-affordability". Similar to the paycheck pattern, Next.js static segments take precedence. The existing `/calculators/[slug]` page will still serve `/calculators/rent-affordability` since "rent-affordability" is a valid slug in the Velite calculators collection. The `[city]` nested route handles the city-specific sub-pages.

Actually, the route structure needs careful handling. The existing route is `/calculators/[slug]/page.tsx`. To add `/calculators/rent-affordability/[city]/page.tsx`, create a new directory `src/app/calculators/rent-affordability/[city]/page.tsx`. This creates a route conflict: Next.js will try to match `/calculators/rent-affordability` against both `[slug]` (slug="rent-affordability") and the new static `rent-affordability` directory.

To resolve: Create `src/app/calculators/rent-affordability/page.tsx` that renders the same content as what `[slug]` would render for "rent-affordability". Then the `[slug]` route's `generateStaticParams` should exclude "rent-affordability" (filter it out) since the dedicated directory handles it. Check if there is a `content/calculators/rent-affordability.yaml` — if yes, read from it in the new dedicated page.

8. **notFound()** for missing city slugs.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/calculators/rent-affordability/[city]/page.tsx exists
    - File contains `export function generateStaticParams(`
    - File contains `export const revalidate = 86400`
    - File contains `FaqSchema`
    - File imports `cities` from `#site/content`
    - File contains median rent table rendering (studio, oneBed, etc.)
    - File contains `notFound()`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>25 city-specific rent calculator pages render with median rent data, cost context, and city navigation</done>
</task>

</tasks>

<verification>
- `npx velite build` validates 25 city YAML files
- `npx tsc --noEmit` passes
- City pages accessible at /calculators/rent-affordability/[city]
- Median rent tables display correct data per city
</verification>

<success_criteria>
- 25 city rent calculator pages accessible at /calculators/rent-affordability/[city] (PSEO-07)
- Each page shows city-specific median rent data and local cost context (PSEO-08)
- Content is genuinely unique per city
- ISR configured for daily revalidation
</success_criteria>

<output>
After completion, create `.planning/phases/06-programmatic-seo-life-stage-hubs/06-07-SUMMARY.md`
</output>
