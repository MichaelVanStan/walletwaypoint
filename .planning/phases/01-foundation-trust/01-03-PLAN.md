---
phase: 01-foundation-trust
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/app/page.tsx
  - src/app/calculators/page.tsx
  - src/app/guides/page.tsx
  - src/app/hubs/page.tsx
  - src/app/compare/page.tsx
  - src/app/not-found.tsx
  - src/app/error.tsx
autonomous: true
requirements:
  - TRUST-02

must_haves:
  truths:
    - "Homepage displays hero with headline, subheading, primary CTA, secondary CTA, and trust signals"
    - "Four placeholder pages exist at /calculators, /guides, /hubs, /compare with descriptive content and Coming Soon badges"
    - "404 page renders friendly message with link back to homepage"
    - "Error page renders recovery message"
    - "All pages have correct meta tags (title, description, canonical URL)"
    - "Disclaimer banner appears on calculator and guide route pages"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage with hero, CTAs, trust signals, placeholder cards"
      contains: "Navigate money with confidence"
    - path: "src/app/calculators/page.tsx"
      provides: "Calculators placeholder page"
      contains: "Financial Calculators"
    - path: "src/app/guides/page.tsx"
      provides: "Guides placeholder page"
      contains: "Financial Guides"
    - path: "src/app/hubs/page.tsx"
      provides: "Hubs placeholder page"
      contains: "Life Stage Hubs"
    - path: "src/app/compare/page.tsx"
      provides: "Compare placeholder page"
      contains: "Product Comparisons"
    - path: "src/app/not-found.tsx"
      provides: "Custom 404 page"
      contains: "Page not found"
    - path: "src/app/error.tsx"
      provides: "Custom error page"
      contains: "Something went wrong"
  key_links:
    - from: "src/app/page.tsx"
      to: "/calculators"
      via: "Primary CTA link"
      pattern: "href.*calculators"
    - from: "src/app/page.tsx"
      to: "/guides"
      via: "Secondary CTA link"
      pattern: "href.*guides"
    - from: "src/app/calculators/page.tsx"
      to: "src/components/trust/disclaimer-banner.tsx"
      via: "Disclaimer banner on calculator route"
      pattern: "DisclaimerBanner"
---

<objective>
Build the homepage with hero section, CTAs, trust signals, and placeholder cards, plus all four placeholder landing pages (/calculators, /guides, /hubs, /compare) and error pages (404, generic error). Each page has proper metadata via createMetadata helper. Disclaimer banner is applied to calculator and guide routes.

Purpose: The homepage is the first impression and primary entry point. Placeholder pages establish the full URL structure for crawlers from day one (per D-05), creating the site architecture that future phases fill in. Error pages provide graceful fallbacks.

Output: 7 fully rendered pages with correct metadata, proper navigation structure, and the disclaimer banner on relevant routes.
</objective>

<execution_context>
@C:/Users/blink-llm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/blink-llm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-trust/01-CONTEXT.md
@.planning/phases/01-foundation-trust/01-RESEARCH.md
@.planning/phases/01-foundation-trust/01-UI-SPEC.md
@.planning/phases/01-foundation-trust/01-01-SUMMARY.md
@.planning/phases/01-foundation-trust/01-02-SUMMARY.md

<interfaces>
From src/lib/metadata.ts:
```typescript
export function createMetadata({ title, description, path, ogImage, ...overrides }: {
  title: string;
  description: string;
  path: string;
  ogImage?: string;
} & Partial<Metadata>): Metadata;
```

From src/lib/site-config.ts:
```typescript
export const siteConfig = {
  name: 'WalletWaypoint',
  tagline: 'Your next financial milestone starts here',
  description: '...',
  url: '...',
  ogImage: '/og.png',
  gaId: '...',
  author: { name: 'WalletWaypoint Editorial Team', role: 'Editorial Team', slug: 'editorial-team' },
} as const;
```

shadcn/ui components available: button, card, badge, alert, separator
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build homepage with hero, CTAs, trust signals, and placeholder cards</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (current placeholder from Plan 01)
    - src/lib/metadata.ts (createMetadata API)
    - src/lib/site-config.ts (siteConfig values)
    - src/components/ui/button.tsx (button component API and variants)
    - src/components/ui/card.tsx (card component API)
    - src/components/ui/badge.tsx (badge component API)
    - .planning/phases/01-foundation-trust/01-UI-SPEC.md (Copywriting Contract - Homepage section, Layout Contract - Content Area, Color section - Accent Reserved For list, Typography section)
  </read_first>
  <action>
    Replace `src/app/page.tsx` with the full homepage. This is a Server Component (no "use client").

    **Metadata export:** Use `createMetadata` with:
    - title: "WalletWaypoint - Your next financial milestone starts here"
    - description: "Interactive calculators, comparison tools, and educational guides for every life stage."
    - path: "/"

    **Hero Section** (per D-08, UI-SPEC Copywriting):
    - Full-width section with `py-16 md:py-24 lg:py-32` vertical padding
    - Max-width container: `max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 text-center`
    - Display headline: "Navigate money with confidence" in `text-2xl md:text-[28px] lg:text-4xl font-semibold text-foreground leading-[1.15]` (responsive scaling per UI-SPEC Typography)
    - Subheading: "Free calculators, expert guides, and comparison tools for every financial milestone -- from your first paycheck to retirement." in `text-base text-muted-foreground mt-4 max-w-2xl mx-auto leading-[1.6]`
    - CTA buttons container: `mt-8 flex flex-col sm:flex-row gap-3 justify-center`
      - Primary CTA: `<Button asChild>` wrapping `<Link href="/calculators">` with text "Explore Calculators". Use accent background: `className="bg-accent text-accent-foreground hover:bg-accent/90"` per UI-SPEC Accent Reserved list item 1
      - Secondary CTA: `<Button variant="outline" asChild>` wrapping `<Link href="/guides">` with text "Read Our Guides"

    **Placeholder Cards Section** (per D-08):
    - Section with `py-12 md:py-16` padding, `max-w-5xl mx-auto px-4 sm:px-6 lg:px-8`
    - Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 md:gap-6`
    - Four cards, one for each placeholder category:
      1. Calculators: "Interactive tools to help you plan mortgages, savings, loans, and more."
      2. Guides: "Clear, jargon-free explanations of the financial topics that matter most."
      3. Life Stage Hubs: "Financial tools and guides organized by where you are in life."
      4. Comparisons: "Side-by-side comparisons of credit cards, loans, and financial products."
    - Each card: `<Card>` with `bg-muted` background, no hover effects (per UI-SPEC Interaction), containing:
      - `<Badge variant="secondary">` with text "Coming Soon" per UI-SPEC Copywriting
      - Card title in `font-semibold text-foreground`
      - Description in `text-sm text-muted-foreground`

    **Trust Signals Section** (per D-08 UI-SPEC Copywriting):
    - Section with `py-12 md:py-16 bg-secondary` background
    - `max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 text-center`
    - Heading: "Built on transparency" in `text-xl font-semibold text-foreground mb-8`
    - Grid: `grid grid-cols-1 md:grid-cols-3 gap-6`
    - Three trust signal cards (non-interactive, static per UI-SPEC Interaction):
      1. "Independent research -- no sponsored rankings"
      2. "Clear methodology -- see how we create content"
      3. "No financial advice -- educational tools you can trust"
    - Each card: `<Card>` with `bg-background` background, containing title text in `text-sm font-medium text-foreground` and icon from lucide-react (Shield, BookOpen, Scale respectively)

    **Import Link from next/link for CTAs.**
  </action>
  <verify>
    <automated>cd "C:/Users/blink-llm/Documents/Development/walletwaypoint" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx contains "Navigate money with confidence"
    - src/app/page.tsx contains "Explore Calculators" and "Read Our Guides"
    - src/app/page.tsx contains "href=\"/calculators\"" and "href=\"/guides\""
    - src/app/page.tsx contains "Coming Soon" and "Badge"
    - src/app/page.tsx contains "Built on transparency"
    - src/app/page.tsx contains "Independent research" and "Clear methodology" and "No financial advice"
    - src/app/page.tsx contains "createMetadata"
    - src/app/page.tsx does NOT contain "use client"
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>Homepage renders hero with headline and CTAs, four Coming Soon placeholder cards, and three trust signal cards. All copy matches UI-SPEC Copywriting Contract exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Build placeholder pages, error pages, and disclaimer banner integration</name>
  <files>src/app/calculators/page.tsx, src/app/guides/page.tsx, src/app/hubs/page.tsx, src/app/compare/page.tsx, src/app/not-found.tsx, src/app/error.tsx, src/app/calculators/layout.tsx, src/app/guides/layout.tsx</files>
  <read_first>
    - src/lib/metadata.ts (createMetadata API)
    - src/lib/site-config.ts (siteConfig values)
    - src/components/trust/disclaimer-banner.tsx (if exists from Plan 04 -- if not, create it here)
    - src/components/ui/button.tsx (button variants)
    - src/components/ui/card.tsx (card component)
    - .planning/phases/01-foundation-trust/01-UI-SPEC.md (Copywriting Contract - Placeholder Pages, Error States, Disclaimer Banner position)
    - .planning/phases/01-foundation-trust/01-RESEARCH.md (Pitfall 6: Placeholder pages, Code Example: DisclaimerBanner)
  </read_first>
  <action>
    1. Create `src/components/trust/disclaimer-banner.tsx` per RESEARCH code example and UI-SPEC:
       ```typescript
       import { Alert, AlertDescription } from '@/components/ui/alert';
       import { Info } from 'lucide-react';
       import Link from 'next/link';

       export function DisclaimerBanner() {
         return (
           <div role="note" aria-label="Educational content disclaimer" className="border-b border-border bg-muted/50">
             <div className="mx-auto flex max-w-5xl items-center gap-2 px-4 py-2 sm:px-6 lg:px-8">
               <Info className="h-4 w-4 shrink-0 text-muted-foreground" />
               <p className="text-sm text-muted-foreground">
                 For educational purposes only — not financial advice.{' '}
                 <Link href="/editorial-standards" className="underline hover:text-foreground">
                   Learn about our editorial process
                 </Link>
               </p>
             </div>
           </div>
         );
       }
       ```
       Per UI-SPEC Layout Contract: positioned at top of content area, below header, as a full-width muted bar. Not dismissible. Uses `role="note"` and `aria-label` per UI-SPEC Accessibility.

    2. Create route-level layouts for disclaimer banner application:

       `src/app/calculators/layout.tsx`:
       ```typescript
       import { DisclaimerBanner } from '@/components/trust/disclaimer-banner';

       export default function CalculatorsLayout({ children }: { children: React.ReactNode }) {
         return (
           <>
             <DisclaimerBanner />
             {children}
           </>
         );
       }
       ```

       `src/app/guides/layout.tsx` -- identical pattern with `DisclaimerBanner`.

       Per UI-SPEC: Disclaimer banner shows on calculator and guide page routes. NOT shown on homepage, trust pages, or placeholder pages for hubs/compare.

    3. Create four placeholder pages, each as a Server Component with static metadata:

       **`src/app/calculators/page.tsx`:**
       - Metadata: title "Financial Calculators", description "Interactive tools to help you plan mortgages, savings, loans, and more. We are building 10 calculators that update in real time -- no spreadsheets needed.", path "/calculators"
       - Content per UI-SPEC Copywriting: H1 "Financial Calculators", description paragraph, "Back to Home" button (`<Button variant="outline" asChild><Link href="/">Back to Home</Link></Button>`), cross-links to trust pages (About, Editorial Standards)
       - Centered layout: `max-w-[720px] mx-auto px-4 sm:px-6 py-12 md:py-16`
       - Badge "Coming Soon" at top

       **`src/app/guides/page.tsx`:**
       - Metadata: title "Financial Guides", description "Clear, jargon-free explanations of the financial topics that matter most. Written like a smart friend who happens to know money.", path "/guides"
       - Same layout pattern as calculators. H1 "Financial Guides". Cross-links to trust pages.

       **`src/app/hubs/page.tsx`:**
       - Metadata: title "Life Stage Hubs", description "Financial tools and guides organized by where you are in life -- from student loans to retirement planning.", path "/hubs"
       - Same layout pattern. H1 "Life Stage Hubs". Cross-links.

       **`src/app/compare/page.tsx`:**
       - Metadata: title "Product Comparisons", description "Side-by-side comparisons of credit cards, loans, and financial products with transparent ranking methodology.", path "/compare"
       - Same layout pattern. H1 "Product Comparisons". Cross-links.

       Each placeholder includes: heading, 2-3 sentence description, "Back to Home" button, cross-links to About and Editorial Standards per UI-SPEC.

    4. Create `src/app/not-found.tsx` per UI-SPEC Error States:
       - H1: "Page not found"
       - Body: "The page you are looking for does not exist or has been moved. Head back to the homepage or try one of the links below."
       - `<Button asChild><Link href="/">Go to Homepage</Link></Button>`
       - Links to: /about, /calculators, /guides
       - Centered layout, `max-w-[720px] mx-auto px-4 sm:px-6 py-12`

    5. Create `src/app/error.tsx` -- must be a `"use client"` component per Next.js requirement:
       - H1: "Something went wrong"
       - Body: "We hit an unexpected error. Try refreshing the page. If the problem continues, let us know."
       - "Let us know" links to `/about#contact`
       - `<Button onClick={() => reset()}>Try again</Button>` using the `reset` prop from Next.js error boundary
       - Props: `{ error: Error & { digest?: string }; reset: () => void }`
  </action>
  <verify>
    <automated>cd "C:/Users/blink-llm/Documents/Development/walletwaypoint" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/trust/disclaimer-banner.tsx contains "For educational purposes only" and "role=\"note\"" and "aria-label" and "editorial-standards"
    - src/app/calculators/layout.tsx contains "DisclaimerBanner"
    - src/app/guides/layout.tsx contains "DisclaimerBanner"
    - src/app/calculators/page.tsx contains "Financial Calculators" and "createMetadata" and "Coming Soon" and "Back to Home"
    - src/app/guides/page.tsx contains "Financial Guides" and "createMetadata" and "Coming Soon"
    - src/app/hubs/page.tsx contains "Life Stage Hubs" and "createMetadata" and "Coming Soon"
    - src/app/compare/page.tsx contains "Product Comparisons" and "createMetadata" and "Coming Soon"
    - src/app/not-found.tsx contains "Page not found" and "Go to Homepage"
    - src/app/error.tsx contains "use client" and "Something went wrong" and "reset"
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>Four placeholder pages render with descriptive content, Coming Soon badges, and metadata. Disclaimer banner appears on /calculators and /guides routes. 404 and error pages provide graceful fallbacks. All pages have canonical URLs via createMetadata.</done>
</task>

</tasks>

<verification>
1. `npx next build` succeeds
2. Homepage at / shows hero, CTAs, placeholder cards, trust signals
3. /calculators shows disclaimer banner, Coming Soon badge, descriptive content
4. /guides shows disclaimer banner, Coming Soon badge, descriptive content
5. /hubs shows Coming Soon badge, descriptive content, NO disclaimer banner
6. /compare shows Coming Soon badge, descriptive content, NO disclaimer banner
7. /nonexistent-page shows custom 404 with "Page not found"
8. All pages have correct title and description in HTML meta tags
9. All pages have canonical URL in head
</verification>

<success_criteria>
- Homepage renders complete hero, CTA buttons, Coming Soon cards, and trust signals matching UI-SPEC copy exactly
- All 4 placeholder pages have substantive content (not thin), Coming Soon badges, and cross-links
- Disclaimer banner renders on /calculators and /guides routes only
- 404 page renders custom friendly message
- Error page renders with try-again button
- All pages use createMetadata for consistent SEO
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-trust/01-03-SUMMARY.md`
</output>
