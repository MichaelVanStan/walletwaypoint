---
phase: 01-foundation-trust
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/layout.tsx
  - src/components/layout/header.tsx
  - src/components/layout/footer.tsx
  - src/components/layout/mobile-nav.tsx
  - src/components/layout/breadcrumbs.tsx
  - src/components/seo/json-ld.tsx
  - src/components/seo/breadcrumb-schema.tsx
  - src/components/seo/organization-schema.tsx
  - src/components/seo/article-schema.tsx
  - src/components/seo/faq-schema.tsx
  - src/components/seo/web-app-schema.tsx
  - src/app/sitemap.ts
  - src/app/robots.ts
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05
  - INFRA-07

must_haves:
  truths:
    - "Sticky header with logo and navigation links is visible on desktop"
    - "Hamburger menu opens a slide-out sheet with full navigation on mobile viewports"
    - "Breadcrumb navigation appears on all pages except the homepage"
    - "Multi-column footer with site links, legal links, and financial disclaimer renders on every page"
    - "XML sitemap is accessible at /sitemap.xml listing all site pages"
    - "robots.txt is accessible at /robots.txt referencing the sitemap"
    - "BreadcrumbList JSON-LD schema is present in page source"
    - "Organization JSON-LD schema is present in page source"
  artifacts:
    - path: "src/components/layout/header.tsx"
      provides: "Sticky header with desktop nav and mobile hamburger"
      contains: "sticky"
    - path: "src/components/layout/footer.tsx"
      provides: "Multi-column footer with disclaimer"
      contains: "footerNavigation"
    - path: "src/components/layout/mobile-nav.tsx"
      provides: "Sheet-based mobile navigation"
      contains: "Sheet"
    - path: "src/components/layout/breadcrumbs.tsx"
      provides: "Auto-detecting breadcrumb component with JSON-LD"
      contains: "usePathname"
    - path: "src/components/seo/json-ld.tsx"
      provides: "Generic JSON-LD script renderer"
      exports: ["JsonLd"]
    - path: "src/app/sitemap.ts"
      provides: "Programmatic XML sitemap"
      contains: "MetadataRoute.Sitemap"
    - path: "src/app/robots.ts"
      provides: "Programmatic robots.txt"
      contains: "MetadataRoute.Robots"
  key_links:
    - from: "src/components/layout/header.tsx"
      to: "src/lib/navigation.ts"
      via: "mainNavigation import"
      pattern: "import.*navigation"
    - from: "src/components/layout/footer.tsx"
      to: "src/lib/navigation.ts"
      via: "footerNavigation import"
      pattern: "import.*navigation"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/header.tsx"
      via: "Header component import"
      pattern: "import.*Header"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/footer.tsx"
      via: "Footer component import"
      pattern: "import.*Footer"
---

<objective>
Build all layout shell components (header, footer, mobile nav, breadcrumbs) and SEO infrastructure (JSON-LD schema components, sitemap.ts, robots.ts). Wire header and footer into the root layout so every page gets the full site chrome.

Purpose: The layout shell and SEO plumbing are shared infrastructure used by every page in Phase 1 and all future phases. Homepage, placeholder pages, and trust pages all need the header, footer, breadcrumbs, and schema markup to be in place first.

Output: Complete site shell visible on every page, with crawlable sitemap, robots.txt, and JSON-LD structured data components ready for use.
</objective>

<execution_context>
@C:/Users/blink-llm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/blink-llm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-trust/01-CONTEXT.md
@.planning/phases/01-foundation-trust/01-RESEARCH.md
@.planning/phases/01-foundation-trust/01-UI-SPEC.md
@.planning/phases/01-foundation-trust/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan depends on -->

From src/types/index.ts:
```typescript
export interface NavItem {
  title: string;
  href: string;
  description?: string;
  disabled?: boolean;
  children?: NavItem[];
}

export interface FooterNavigation {
  tools: NavItem[];
  learn: NavItem[];
  company: NavItem[];
  legal: NavItem[];
}
```

From src/lib/site-config.ts:
```typescript
export const siteConfig = {
  name: 'WalletWaypoint',
  tagline: 'Your next financial milestone starts here',
  description: '...',
  url: process.env.NEXT_PUBLIC_SITE_URL || 'https://walletwaypoint.com',
  ogImage: '/og.png',
  gaId: process.env.NEXT_PUBLIC_GA_ID || '',
  author: { name: '...', role: '...', slug: '...' },
} as const;
```

From src/lib/navigation.ts:
```typescript
export const mainNavigation: NavItem[];  // Calculators, Guides, Life Stage Hubs, Compare (all disabled)
export const footerNavigation: FooterNavigation;  // tools, learn, company, legal columns
```

From src/lib/metadata.ts:
```typescript
export function createMetadata({ title, description, path, ogImage, ...overrides }: { ... }): Metadata;
```

shadcn/ui components available: button, navigation-menu, sheet, breadcrumb, card, badge, separator, accordion, alert, tooltip
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build layout components (header, footer, mobile nav, breadcrumbs) and wire into root layout</name>
  <files>src/components/layout/header.tsx, src/components/layout/footer.tsx, src/components/layout/mobile-nav.tsx, src/components/layout/breadcrumbs.tsx, src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (current root layout from Plan 01)
    - src/lib/navigation.ts (navigation data structure)
    - src/lib/site-config.ts (site name, URL)
    - src/types/index.ts (NavItem, FooterNavigation)
    - src/components/ui/navigation-menu.tsx (shadcn component API)
    - src/components/ui/sheet.tsx (shadcn component API)
    - src/components/ui/breadcrumb.tsx (shadcn component API)
    - src/components/ui/button.tsx (shadcn component API)
    - src/components/ui/separator.tsx (shadcn component API)
    - .planning/phases/01-foundation-trust/01-UI-SPEC.md (Layout Contract, Interaction Contract, Accessibility Contract, Component Inventory)
  </read_first>
  <action>
    1. Create `src/components/layout/header.tsx` -- a Server Component wrapper with a Client Component inner for mobile state:

       **Desktop (md+ breakpoint):** Sticky top bar (`sticky top-0 z-50 backdrop-blur-sm bg-background/95 border-b border-border`). Height: 64px desktop, 56px mobile per UI-SPEC. Left: text logo "WalletWaypoint" in `text-primary font-semibold text-lg` linking to "/". Center/right: navigation links from `mainNavigation` using shadcn `NavigationMenu`. Disabled items (`disabled: true`) render in `text-muted-foreground cursor-default` with "(Coming Soon)" appended, no hover effect. Enabled items have hover transition: `text-muted-foreground` to `text-foreground` over 150ms.

       **Mobile (< md):** Logo on left, hamburger icon button on right with `aria-label="Open navigation menu"`. Hamburger click opens `MobileNav` sheet component.

       **Accessibility:** `<header>` element wrapping a `<nav>` element. Skip navigation link as first focusable element: `<a href="#main-content" className="sr-only focus:not-sr-only focus:absolute focus:z-[100] focus:bg-background focus:px-4 focus:py-2 focus:text-foreground">Skip to main content</a>`

    2. Create `src/components/layout/mobile-nav.tsx` -- a `"use client"` component:
       - Uses shadcn `Sheet` component with `side="left"` per UI-SPEC
       - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`
       - Renders full navigation list from `mainNavigation` as vertical link list
       - Disabled items show "(Coming Soon)" badge in muted text
       - Each link click calls `onOpenChange(false)` to close the sheet
       - Close button via shadcn Sheet's built-in close
       - Respects `prefers-reduced-motion: reduce` per UI-SPEC Accessibility Contract

    3. Create `src/components/layout/footer.tsx` -- a Server Component:
       - Background: `bg-secondary` per UI-SPEC
       - Padding: 48px vertical per UI-SPEC
       - 4-column grid on lg+ (tools, learn, company, legal) from `footerNavigation`. 2-column on md. Stacked on mobile.
       - Each column: heading in `text-sm font-semibold text-foreground mb-3`, links in `text-sm text-muted-foreground` with underline on hover and transition to `text-foreground` over 150ms per UI-SPEC Interaction Contract
       - Below columns: `Separator` component, then full-width financial disclaimer in `text-xs text-muted-foreground`:
         "WalletWaypoint provides educational content and tools for informational purposes only. Nothing on this site constitutes financial, tax, or legal advice. Consult a qualified professional before making financial decisions."
       - Copyright line at bottom: "2024-{current year} WalletWaypoint. All rights reserved."
       - `<footer>` semantic element per UI-SPEC Accessibility Contract

    4. Create `src/components/layout/breadcrumbs.tsx` -- a `"use client"` component (needs `usePathname`):
       - Auto-detects current route via `usePathname()` from `next/navigation`
       - Splits pathname into segments and builds breadcrumb items
       - First item is always "Home" linking to "/"
       - Capitalizes and de-slugifies segment names (e.g., "editorial-standards" becomes "Editorial Standards")
       - Last segment is current page (non-linked, `text-foreground`)
       - Previous items are links in `text-muted-foreground` with underline on hover per UI-SPEC
       - Uses shadcn `Breadcrumb`, `BreadcrumbItem`, `BreadcrumbLink`, `BreadcrumbList`, `BreadcrumbPage`, `BreadcrumbSeparator` components
       - Wrapped in `<nav aria-label="Breadcrumb">` per UI-SPEC Accessibility Contract
       - Does NOT render on the homepage (when pathname is "/")
       - Also renders `BreadcrumbSchema` JSON-LD component (from Task 2) alongside visual breadcrumbs
       - Optional `items` prop to override auto-detection: `items?: { name: string; href: string }[]`

    5. Update `src/app/layout.tsx` to:
       - Import and render `Header` above `<main>`
       - Import and render `Footer` below `<main>`
       - Import and render `Breadcrumbs` inside `<main>` above `{children}`
       - Add `id="main-content"` to `<main>` element for skip navigation target
       - Ensure landmark structure: `<header>`, `<main>`, `<footer>` per UI-SPEC Accessibility
  </action>
  <verify>
    <automated>cd "C:/Users/blink-llm/Documents/Development/walletwaypoint" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/header.tsx contains "sticky top-0 z-50" and "WalletWaypoint" and "mainNavigation" and "NavigationMenu" and "Skip to main content" and "aria-label"
    - src/components/layout/footer.tsx contains "footerNavigation" and "bg-secondary" and "educational content and tools for informational purposes only" and "Separator"
    - src/components/layout/mobile-nav.tsx contains "use client" and "Sheet" and "side=\"left\"" and "onOpenChange" and "Coming Soon"
    - src/components/layout/breadcrumbs.tsx contains "use client" and "usePathname" and "BreadcrumbList" and "aria-label=\"Breadcrumb\"" and "BreadcrumbSchema"
    - src/app/layout.tsx contains "Header" and "Footer" and "Breadcrumbs" and "id=\"main-content\""
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>Header, footer, mobile nav, and breadcrumbs render on every page. Skip navigation link present. Footer shows financial disclaimer. Mobile hamburger opens sheet navigation. Breadcrumbs auto-detect route and do not appear on homepage.</done>
</task>

<task type="auto">
  <name>Task 2: Build SEO infrastructure (JSON-LD schema components, sitemap.ts, robots.ts)</name>
  <files>src/components/seo/json-ld.tsx, src/components/seo/breadcrumb-schema.tsx, src/components/seo/organization-schema.tsx, src/components/seo/article-schema.tsx, src/components/seo/faq-schema.tsx, src/components/seo/web-app-schema.tsx, src/app/sitemap.ts, src/app/robots.ts</files>
  <read_first>
    - src/lib/site-config.ts (siteConfig for URLs and names)
    - .planning/phases/01-foundation-trust/01-RESEARCH.md (Pattern 3: JSON-LD, Pattern 4: Sitemap, Pattern 5: Robots, Pitfall 3: XSS, Pitfall 5: FAQPage)
    - .planning/phases/01-foundation-trust/01-UI-SPEC.md (Component Inventory - Custom Components section)
  </read_first>
  <action>
    1. Create `src/components/seo/json-ld.tsx` -- generic JSON-LD renderer per RESEARCH Pattern 3:
       ```typescript
       import type { WithContext, Thing } from 'schema-dts';

       export function JsonLd<T extends Thing>({ data }: { data: WithContext<T> }) {
         return (
           <script
             type="application/ld+json"
             dangerouslySetInnerHTML={{
               __html: JSON.stringify(data).replace(/</g, '\\u003c'),
             }}
           />
         );
       }
       ```
       CRITICAL: The `.replace(/</g, '\\u003c')` is mandatory per RESEARCH Pitfall 3 (XSS prevention). Do NOT omit this.

    2. Create `src/components/seo/breadcrumb-schema.tsx`:
       - Import `JsonLd` from `./json-ld`
       - Props: `items: { name: string; url: string }[]`
       - Renders `BreadcrumbList` JSON-LD with `@context: 'https://schema.org'`, `@type: 'BreadcrumbList'`, `itemListElement` array of `ListItem` objects with `position`, `name`, `item` (url)
       - Per RESEARCH Pattern 3 code exactly

    3. Create `src/components/seo/organization-schema.tsx`:
       - Import `JsonLd` and `siteConfig`
       - No props (reads from siteConfig)
       - Renders Organization JSON-LD with `@type: 'Organization'`, `name: siteConfig.name`, `url: siteConfig.url`, `description: siteConfig.description`
       - This component is rendered once in the root layout (will be wired in after creation)

    4. Create `src/components/seo/article-schema.tsx`:
       - Props: `{ title: string; description: string; url: string; datePublished: string; dateModified: string; authorName: string; authorUrl: string }`
       - Renders Article JSON-LD with all fields mapped to Schema.org Article type
       - Used on trust pages (About, Editorial Standards) in Plan 04

    5. Create `src/components/seo/faq-schema.tsx`:
       - Props: `{ items: { question: string; answer: string }[] }`
       - Renders FAQPage JSON-LD
       - Note per RESEARCH Pitfall 5: FAQPage rich results are restricted since 2023 but still useful for AI engines and structure

    6. Create `src/components/seo/web-app-schema.tsx`:
       - Props: `{ name: string; description: string; url: string; applicationCategory?: string }`
       - Renders WebApplication JSON-LD
       - Used in Phase 2 for calculator pages, built now for readiness

    7. Create `src/app/sitemap.ts` per RESEARCH Pattern 4:
       - Import `MetadataRoute` from 'next'
       - Export default function returning `MetadataRoute.Sitemap` array
       - Use `siteConfig.url` as base URL (import from `@/lib/site-config`)
       - Include all Phase 1 pages:
         - `/` (priority 1, changeFrequency 'weekly')
         - `/about` (priority 0.8, changeFrequency 'monthly')
         - `/editorial-standards` (priority 0.7, changeFrequency 'monthly')
         - `/privacy-policy` (priority 0.3, changeFrequency 'yearly')
         - `/authors/editorial-team` (priority 0.6, changeFrequency 'monthly')
         - `/calculators` (priority 0.9, changeFrequency 'weekly')
         - `/guides` (priority 0.9, changeFrequency 'weekly')
         - `/hubs` (priority 0.9, changeFrequency 'weekly')
         - `/compare` (priority 0.9, changeFrequency 'weekly')
       - All entries use `lastModified: new Date()`

    8. Create `src/app/robots.ts` per RESEARCH Pattern 5:
       - Import `MetadataRoute` from 'next'
       - Import `siteConfig` from `@/lib/site-config`
       - Export default function returning `MetadataRoute.Robots`
       - Rules: userAgent '*', allow '/', disallow ['/api/', '/private/']
       - Sitemap: `${siteConfig.url}/sitemap.xml`

    9. Update `src/app/layout.tsx` to render `OrganizationSchema` component once inside `<body>` (site-wide structured data).
  </action>
  <verify>
    <automated>cd "C:/Users/blink-llm/Documents/Development/walletwaypoint" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/seo/json-ld.tsx contains "application/ld+json" and "replace(/</g" and "\\u003c" and "dangerouslySetInnerHTML"
    - src/components/seo/breadcrumb-schema.tsx contains "BreadcrumbList" and "itemListElement" and "ListItem" and "position"
    - src/components/seo/organization-schema.tsx contains "Organization" and "siteConfig" and "schema.org"
    - src/components/seo/article-schema.tsx contains "Article" and "datePublished" and "author"
    - src/components/seo/faq-schema.tsx contains "FAQPage" and "mainEntity" and "Question" and "Answer"
    - src/components/seo/web-app-schema.tsx contains "WebApplication" and "applicationCategory"
    - src/app/sitemap.ts contains "MetadataRoute.Sitemap" and "/about" and "/editorial-standards" and "/privacy-policy" and "/calculators" and "/guides" and "/hubs" and "/compare"
    - src/app/robots.ts contains "MetadataRoute.Robots" and "sitemap" and "/api/"
    - src/app/layout.tsx contains "OrganizationSchema"
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>All 6 JSON-LD schema components created and ready for use. Sitemap at /sitemap.xml lists all 9 Phase 1 pages. Robots.txt at /robots.txt references sitemap and allows crawling. Organization schema renders site-wide via root layout.</done>
</task>

</tasks>

<verification>
1. `npx next build` succeeds
2. Visit any page -- header with sticky nav, footer with 4-column links and disclaimer visible
3. Visit `/about` -- breadcrumbs show "Home > About", JSON-LD in page source includes BreadcrumbList
4. Visit `/` -- no breadcrumbs shown (homepage)
5. Request `/sitemap.xml` -- returns XML with all 9 page URLs
6. Request `/robots.txt` -- contains sitemap reference and allow/disallow rules
7. View page source -- contains `application/ld+json` script with Organization schema
8. Resize to mobile width -- hamburger icon appears, clicking opens sheet with nav links
</verification>

<success_criteria>
- Header renders on all pages with sticky positioning, logo, and navigation
- Mobile hamburger menu opens Sheet with full nav list
- Footer renders 4-column layout (lg+) with financial disclaimer
- Breadcrumbs auto-detect route on all non-homepage pages
- Sitemap.xml lists all 9 Phase 1 URLs
- Robots.txt references sitemap and blocks /api/ and /private/
- JSON-LD components for BreadcrumbList, Organization, Article, FAQPage, WebApplication exist and are importable
- All JSON-LD uses XSS-safe serialization (replace < with \u003c)
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-trust/01-02-SUMMARY.md`
</output>
