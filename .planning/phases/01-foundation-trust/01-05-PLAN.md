---
phase: 01-foundation-trust
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified: []
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05
  - INFRA-06
  - INFRA-07
  - TRUST-01
  - TRUST-02
  - TRUST-03
  - TRUST-04
  - TRUST-05
  - TRUST-06

must_haves:
  truths:
    - "All pages render as static HTML with correct meta tags and canonical URLs"
    - "Site passes Core Web Vitals (LCP < 2.5s, CLS < 0.1)"
    - "Breadcrumb navigation with BreadcrumbList schema works on all non-homepage pages"
    - "Sitemap.xml and robots.txt are accessible and correct"
    - "Trust pages exist with substantive content and Article schema"
    - "Disclaimer banner appears on calculator and guide routes"
    - "Header, footer, and mobile nav function correctly across breakpoints"
  artifacts: []
  key_links: []
---

<objective>
Final verification of all Phase 1 requirements. Run build, verify static generation, check all pages render correctly, validate SEO infrastructure, and confirm trust pages meet E-E-A-T standards. Human checkpoint for visual verification of layout, responsiveness, and content quality.

Purpose: Phase 1 success criteria require visual and functional verification that automated tests alone cannot cover (responsive layout, readable content, correct visual hierarchy). This checkpoint ensures everything works before moving to Phase 2.

Output: Verified Phase 1 with all 13 requirements confirmed.
</objective>

<execution_context>
@C:/Users/blink-llm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/blink-llm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-trust/01-CONTEXT.md
@.planning/phases/01-foundation-trust/01-UI-SPEC.md
@.planning/phases/01-foundation-trust/01-01-SUMMARY.md
@.planning/phases/01-foundation-trust/01-02-SUMMARY.md
@.planning/phases/01-foundation-trust/01-03-SUMMARY.md
@.planning/phases/01-foundation-trust/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build and verify static generation, SEO infrastructure, and schema markup</name>
  <files></files>
  <read_first>
    - .planning/phases/01-foundation-trust/01-RESEARCH.md (Standard Stack, Pitfalls section)
    - .planning/REQUIREMENTS.md (INFRA-01 through INFRA-07, TRUST-01 through TRUST-06)
  </read_first>
  <action>
    Run the following verification commands sequentially and report results:

    1. `npx next build` -- must succeed. Check output for:
       - All pages listed as "Static" (circle icon) or "SSG" in the route summary. No pages should be "Dynamic" or "Server" rendered.
       - No build warnings about missing metadata or accessibility.

    2. Start production server: `npx next start` (background), then verify:

       **INFRA-01 (Static generation):** All pages return 200 with static HTML:
       ```
       curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/
       curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/about
       curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/editorial-standards
       curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/privacy-policy
       curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/authors/editorial-team
       curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/calculators
       curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/guides
       curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/hubs
       curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/compare
       ```

       **INFRA-02 (Meta tags):** Check pages for title, description, og:title, canonical:
       ```
       curl -s http://localhost:3000/about | grep -o '<title>[^<]*</title>'
       curl -s http://localhost:3000/about | grep -o 'rel="canonical"[^>]*'
       curl -s http://localhost:3000/about | grep -o 'property="og:title"[^>]*'
       ```

       **INFRA-03 (Breadcrumbs + schema):** Verify breadcrumb schema on non-homepage:
       ```
       curl -s http://localhost:3000/about | grep -o 'BreadcrumbList'
       ```

       **INFRA-04 (Sitemap + robots):**
       ```
       curl -s http://localhost:3000/sitemap.xml | head -20
       curl -s http://localhost:3000/robots.txt
       ```

       **INFRA-05 (Schema markup):** Check for Article and Organization schema:
       ```
       curl -s http://localhost:3000/about | grep -o '"@type":"Article"'
       curl -s http://localhost:3000/ | grep -o '"@type":"Organization"'
       curl -s http://localhost:3000/editorial-standards | grep -o '"@type":"FAQPage"'
       ```

       **TRUST-01 (Affiliate disclosure component):** Verify component file exists and is importable.
       **TRUST-02 (Disclaimer):** Verify disclaimer on calculator/guide routes:
       ```
       curl -s http://localhost:3000/calculators | grep -o 'educational purposes only'
       curl -s http://localhost:3000/guides | grep -o 'educational purposes only'
       ```

       **TRUST-03 through TRUST-06:** Verify trust page content length (not thin):
       ```
       curl -s http://localhost:3000/privacy-policy | wc -c  # Should be substantial (> 5000 chars)
       curl -s http://localhost:3000/about | wc -c
       curl -s http://localhost:3000/editorial-standards | wc -c
       curl -s http://localhost:3000/authors/editorial-team | wc -c
       ```

    3. Stop the production server.

    4. Report a summary table of all 13 requirements with PASS/FAIL status.

    5. If any requirement FAILS, create a fix task list and execute fixes before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd "C:/Users/blink-llm/Documents/Development/creditrep" && npx next build 2>&1 | grep -E "(Route|Static|Dynamic|Server|Error)" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `npx next build` exits with code 0
    - Build output shows all routes as Static (not Dynamic or Server)
    - All 9 page URLs return HTTP 200
    - /sitemap.xml returns valid XML with 9 URLs
    - /robots.txt contains "Sitemap:" and "User-agent:"
    - /about page source contains "@type":"Article" and "BreadcrumbList"
    - / page source contains "@type":"Organization"
    - /calculators page source contains "educational purposes only"
    - All trust pages have content length > 5000 characters (not thin content)
  </acceptance_criteria>
  <done>All 13 Phase 1 requirements verified programmatically. Build succeeds, all pages static, SEO infrastructure functional, trust pages substantive.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of visual quality, responsiveness, and content</name>
  <files></files>
  <action>
    Present the following verification checklist to the user. Start dev server with `npm run dev` and provide the URL http://localhost:3000.

    Complete Phase 1 Foundation and Trust implementation includes:
    - Next.js 16 project with blue-based design system (Inter font, OKLCH palette, shadcn/ui)
    - Sticky header with navigation (desktop links, mobile hamburger)
    - Multi-column footer with financial disclaimer
    - Homepage with hero, CTAs, placeholder cards, trust signals
    - 4 placeholder pages (/calculators, /guides, /hubs, /compare) with Coming Soon badges
    - 4 trust pages (/about, /editorial-standards, /privacy-policy, /authors/editorial-team)
    - Disclaimer banner on calculator and guide routes
    - SEO infrastructure (sitemap, robots.txt, JSON-LD schema markup, breadcrumbs)
    - GA4 and Vercel Analytics integration

    Verification checklist for the user:

    **1. Homepage (http://localhost:3000)**
    - [ ] Hero section: headline "Navigate money with confidence", subheading, two CTA buttons
    - [ ] "Explore Calculators" button has teal/green accent color
    - [ ] Four Coming Soon placeholder cards in a grid
    - [ ] Trust signals section with "Built on transparency" heading and three cards
    - [ ] Overall visual impression: clean, professional, blue-based palette

    **2. Navigation**
    - [ ] Sticky header stays on top when scrolling
    - [ ] Desktop: navigation links visible, disabled items show "(Coming Soon)" in muted text
    - [ ] Resize to mobile width: hamburger icon appears, clicking opens slide-out menu from left
    - [ ] Footer: 4 columns of links (Tools, Learn, Company, Legal) with financial disclaimer below

    **3. Placeholder Pages**
    - [ ] /calculators: Shows disclaimer banner at top, Coming Soon badge, descriptive content
    - [ ] /guides: Shows disclaimer banner at top, Coming Soon badge, descriptive content
    - [ ] /hubs: Coming Soon badge, descriptive content, NO disclaimer banner
    - [ ] /compare: Coming Soon badge, descriptive content, NO disclaimer banner
    - [ ] All have "Back to Home" button

    **4. Trust Pages**
    - [ ] /about: Substantive content about mission, audience, differentiation, monetization
    - [ ] /editorial-standards: Methodology, data sources, affiliate disclosure, FAQ accordion
    - [ ] /privacy-policy: All privacy sections readable and complete
    - [ ] /authors/editorial-team: Team profile with editorial process link

    **5. Responsive Layout**
    - [ ] All pages look good on mobile (< 640px), tablet (768px), and desktop (1024px+)
    - [ ] Breadcrumbs appear on all pages except homepage
    - [ ] Text is readable, no overlapping elements, no horizontal scroll

    **6. SEO (view page source)**
    - [ ] Title and meta description present in head on any trust page
    - [ ] JSON-LD script tags present (Organization on homepage, Article on trust pages)
  </action>
  <verify>User confirms "approved" or describes issues to fix</verify>
  <done>User has visually verified Phase 1 implementation and approved it for completion. Phase 1 is ready for Phase 2.</done>
</task>

</tasks>

<verification>
Phase 1 Success Criteria from ROADMAP.md:
1. All pages render as static HTML (SSG/ISR) and are crawlable with correct meta tags, canonical URLs, and OG data
2. Site passes Core Web Vitals (LCP < 2.5s, CLS < 0.1, INP < 200ms) with responsive layout
3. Breadcrumb navigation with BreadcrumbList schema, automated XML sitemap, and robots.txt functioning
4. Trust pages exist and are linked from navigation: About, Editorial Standards, Privacy Policy, disclaimers
5. Author bio pages with credentials, affiliate disclosure component, FAQPage/Article/WebApplication schema markup
</verification>

<success_criteria>
- All 13 requirements (INFRA-01 through INFRA-07, TRUST-01 through TRUST-06) verified
- Build succeeds with all routes static
- Human approves visual quality, responsiveness, and content
- Phase 1 is complete and ready for Phase 2 (Calculator Engine)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-trust/01-05-SUMMARY.md`
</output>
