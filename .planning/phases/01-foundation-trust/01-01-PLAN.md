---
phase: 01-foundation-trust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - .prettierrc
  - .prettierignore
  - .env.local
  - .env.example
  - src/app/globals.css
  - src/app/layout.tsx
  - src/lib/site-config.ts
  - src/lib/metadata.ts
  - src/lib/navigation.ts
  - src/types/index.ts
  - components.json
autonomous: true
requirements:
  - INFRA-01
  - INFRA-06
  - INFRA-07

must_haves:
  truths:
    - "Next.js dev server starts without errors on localhost:3000"
    - "All pages render as static HTML by default (no dynamic APIs used)"
    - "Inter font loads with zero layout shift via next/font CSS variable"
    - "Tailwind CSS 4 theme applies blue-based OKLCH palette correctly"
    - "GA4 and Vercel Analytics scripts are present in rendered HTML"
    - "shadcn/ui components are installed and importable"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies"
      contains: "next"
    - path: "src/app/globals.css"
      provides: "Tailwind CSS 4 theme with OKLCH color variables"
      contains: "@theme inline"
    - path: "src/app/layout.tsx"
      provides: "Root layout with Inter font, analytics, html lang"
      contains: "Inter"
    - path: "src/lib/site-config.ts"
      provides: "Site-wide constants"
      contains: "WalletWaypoint"
    - path: "src/lib/metadata.ts"
      provides: "Shared metadata helper"
      exports: ["createMetadata"]
    - path: "src/lib/navigation.ts"
      provides: "Navigation structure for header/footer"
      exports: ["mainNavigation", "footerNavigation"]
    - path: "src/types/index.ts"
      provides: "Shared TypeScript types"
      exports: ["NavItem"]
    - path: "components.json"
      provides: "shadcn/ui configuration"
      contains: "shadcn"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals\\.css"
    - from: "src/app/layout.tsx"
      to: "next/font/google"
      via: "Inter font import"
      pattern: "Inter"
    - from: "src/lib/metadata.ts"
      to: "src/lib/site-config.ts"
      via: "siteConfig import"
      pattern: "import.*site-config"

user_setup:
  - service: google-analytics
    why: "GA4 tracking from day one per D-13 (required for Mediavine qualification)"
    env_vars:
      - name: NEXT_PUBLIC_GA_ID
        source: "Google Analytics -> Admin -> Data Streams -> Measurement ID (G-XXXXXXXXXX)"
    dashboard_config:
      - task: "Create GA4 property for WalletWaypoint"
        location: "Google Analytics -> Admin -> Create Property"
---

<objective>
Scaffold the Next.js 16 project with the complete design system foundation: Tailwind CSS 4 blue-based OKLCH theme, Inter font via next/font, shadcn/ui initialization with Phase 1 components, analytics integration (GA4 + Vercel), and all shared configuration files (site config, metadata helper, navigation structure, TypeScript types).

Purpose: Every subsequent plan depends on this foundation existing. The project skeleton, design tokens, and shared utilities must be in place before any pages or components can be built.

Output: A running Next.js 16 dev server with the complete design system, analytics, and shared configuration ready for component and page development.
</objective>

<execution_context>
@C:/Users/blink-llm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/blink-llm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-trust/01-CONTEXT.md
@.planning/phases/01-foundation-trust/01-RESEARCH.md
@.planning/phases/01-foundation-trust/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project and install all Phase 1 dependencies</name>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs, .prettierrc, .prettierignore, .env.local, .env.example, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation-trust/01-RESEARCH.md (Installation section, Standard Stack table, Environment Availability)
    - .planning/phases/01-foundation-trust/01-UI-SPEC.md (Design System table, Registry Safety)
    - CLAUDE.md (Technology Stack section)
  </read_first>
  <action>
    1. Create the Next.js 16 project using:
       ```
       npx create-next-app@latest walletwaypoint --typescript --tailwind --eslint --app --src-dir --yes
       ```
       Then move all generated files from the `walletwaypoint/` subdirectory up to the project root (since we are already in the project root directory). Remove the empty `walletwaypoint/` directory.

    2. Install core production dependencies:
       ```
       npm install schema-dts @next/third-parties @vercel/analytics @vercel/speed-insights sharp
       ```

    3. Install dev dependencies:
       ```
       npm install -D eslint-plugin-jsx-a11y prettier prettier-plugin-tailwindcss
       ```

    4. Initialize shadcn/ui:
       ```
       npx shadcn@latest init
       ```
       Accept defaults (Tailwind v4 mode, OKLCH colors, Radix primitives). This creates `components.json` and `src/components/ui/` directory.

    5. Install all Phase 1 shadcn/ui components in a single command:
       ```
       npx shadcn@latest add button navigation-menu sheet breadcrumb card badge separator accordion alert tooltip
       ```

    6. Create `.prettierrc` with content:
       ```json
       {
         "semi": true,
         "singleQuote": true,
         "tabWidth": 2,
         "trailingComma": "all",
         "plugins": ["prettier-plugin-tailwindcss"]
       }
       ```

    7. Create `.prettierignore` with content:
       ```
       node_modules
       .next
       dist
       build
       coverage
       ```

    8. Update `next.config.ts` to set `trailingSlash: false` (per RESEARCH.md Pitfall 1 -- canonical URL consistency):
       ```typescript
       import type { NextConfig } from 'next';

       const nextConfig: NextConfig = {
         trailingSlash: false,
       };

       export default nextConfig;
       ```

    9. Create `.env.local` with:
       ```
       NEXT_PUBLIC_GA_ID=
       NEXT_PUBLIC_SITE_URL=http://localhost:3000
       ```

    10. Create `.env.example` with same keys (no values) for documentation:
        ```
        NEXT_PUBLIC_GA_ID=
        NEXT_PUBLIC_SITE_URL=http://localhost:3000
        ```

    11. Ensure `.gitignore` includes `.env.local` (create-next-app should handle this, but verify).
  </action>
  <verify>
    <automated>cd "C:/Users/blink-llm/Documents/Development/walletwaypoint" && node -e "const p = require('./package.json'); const deps = {...p.dependencies, ...p.devDependencies}; const required = ['next','react','typescript','tailwindcss','schema-dts','@next/third-parties','@vercel/analytics','@vercel/speed-insights','sharp','eslint-plugin-jsx-a11y','prettier','prettier-plugin-tailwindcss']; const missing = required.filter(r => !deps[r]); if(missing.length) { console.error('Missing:', missing); process.exit(1); } console.log('All dependencies present');"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next", "react", "typescript", "tailwindcss", "schema-dts", "@next/third-parties", "@vercel/analytics", "@vercel/speed-insights", "sharp"
    - package.json devDependencies contains "eslint-plugin-jsx-a11y", "prettier", "prettier-plugin-tailwindcss"
    - components.json exists in project root (shadcn/ui initialized)
    - src/components/ui/ directory contains button.tsx, navigation-menu.tsx, sheet.tsx, breadcrumb.tsx, card.tsx, badge.tsx, separator.tsx, accordion.tsx, alert.tsx, tooltip.tsx
    - .prettierrc contains "prettier-plugin-tailwindcss"
    - next.config.ts contains "trailingSlash: false"
    - .env.local contains "NEXT_PUBLIC_GA_ID" and "NEXT_PUBLIC_SITE_URL"
    - .env.example contains "NEXT_PUBLIC_GA_ID" and "NEXT_PUBLIC_SITE_URL"
  </acceptance_criteria>
  <done>Next.js 16 project created with all Phase 1 dependencies installed, shadcn/ui initialized with 10 components, linting/formatting configured, environment variables set up</done>
</task>

<task type="auto">
  <name>Task 2: Configure design system (Tailwind theme, globals.css, Inter font) and create root layout with analytics</name>
  <files>src/app/globals.css, src/app/layout.tsx, src/lib/site-config.ts, src/lib/metadata.ts, src/lib/navigation.ts, src/types/index.ts</files>
  <read_first>
    - src/app/globals.css (current state after shadcn init)
    - src/app/layout.tsx (current state after create-next-app)
    - .planning/phases/01-foundation-trust/01-UI-SPEC.md (Color section, Typography section, Layout Contract, Spacing Scale)
    - .planning/phases/01-foundation-trust/01-RESEARCH.md (Pattern 1 through Pattern 6, Navigation Structure code)
    - .planning/phases/01-foundation-trust/01-CONTEXT.md (D-01 through D-14)
  </read_first>
  <action>
    1. Replace `src/app/globals.css` with the full Tailwind CSS 4 theme per UI-SPEC and RESEARCH Pattern 6. The file must contain:
       ```css
       @import "tailwindcss";
       @import "tw-animate-css";

       @custom-variant dark (&:is(.dark *));

       :root {
         --background: oklch(0.99 0.002 240);
         --foreground: oklch(0.15 0.02 240);
         --primary: oklch(0.35 0.1 245);
         --primary-foreground: oklch(0.98 0.005 240);
         --secondary: oklch(0.93 0.01 240);
         --secondary-foreground: oklch(0.25 0.04 240);
         --accent: oklch(0.55 0.12 175);
         --accent-foreground: oklch(0.98 0.005 175);
         --muted: oklch(0.95 0.005 240);
         --muted-foreground: oklch(0.55 0.02 240);
         --destructive: oklch(0.55 0.2 25);
         --border: oklch(0.88 0.01 240);
         --ring: oklch(0.35 0.1 245);
         --radius: 0.5rem;
         --chart-1: oklch(0.35 0.1 245);
         --chart-2: oklch(0.55 0.12 175);
         --chart-3: oklch(0.65 0.1 140);
         --chart-4: oklch(0.45 0.08 260);
         --chart-5: oklch(0.75 0.08 200);
       }

       .dark {
         --background: oklch(0.12 0.02 240);
         --foreground: oklch(0.95 0.005 240);
         --primary: oklch(0.65 0.12 245);
         --primary-foreground: oklch(0.12 0.02 240);
         --secondary: oklch(0.2 0.015 240);
         --secondary-foreground: oklch(0.9 0.005 240);
         --accent: oklch(0.6 0.12 175);
         --accent-foreground: oklch(0.12 0.02 175);
         --muted: oklch(0.2 0.01 240);
         --muted-foreground: oklch(0.65 0.02 240);
         --destructive: oklch(0.55 0.2 25);
         --border: oklch(0.3 0.015 240);
         --ring: oklch(0.65 0.12 245);
         --chart-1: oklch(0.65 0.12 245);
         --chart-2: oklch(0.6 0.12 175);
         --chart-3: oklch(0.7 0.1 140);
         --chart-4: oklch(0.55 0.08 260);
         --chart-5: oklch(0.8 0.08 200);
       }

       @theme inline {
         --color-background: var(--background);
         --color-foreground: var(--foreground);
         --color-primary: var(--primary);
         --color-primary-foreground: var(--primary-foreground);
         --color-secondary: var(--secondary);
         --color-secondary-foreground: var(--secondary-foreground);
         --color-accent: var(--accent);
         --color-accent-foreground: var(--accent-foreground);
         --color-muted: var(--muted);
         --color-muted-foreground: var(--muted-foreground);
         --color-destructive: var(--destructive);
         --color-border: var(--border);
         --color-ring: var(--ring);
         --radius-sm: calc(var(--radius) - 4px);
         --radius-md: calc(var(--radius) - 2px);
         --radius-lg: var(--radius);
         --radius-xl: calc(var(--radius) + 4px);
         --font-sans: var(--font-inter), ui-sans-serif, system-ui, sans-serif;
       }
       ```
       Note: dark mode variables are included for future-proofing per RESEARCH Open Question 3, but NO toggle or prefers-color-scheme detection ships in Phase 1.

    2. Create `src/lib/site-config.ts`:
       ```typescript
       export const siteConfig = {
         name: 'WalletWaypoint',
         tagline: 'Your next financial milestone starts here',
         description: 'Interactive calculators, comparison tools, and educational guides for every life stage.',
         url: process.env.NEXT_PUBLIC_SITE_URL || 'https://walletwaypoint.com',
         ogImage: '/og.png',
         gaId: process.env.NEXT_PUBLIC_GA_ID || '',
         author: {
           name: 'WalletWaypoint Editorial Team',
           role: 'Editorial Team',
           slug: 'editorial-team',
         },
       } as const;
       ```

    3. Create `src/lib/metadata.ts` with the `createMetadata` helper per RESEARCH Pattern 1. The function signature:
       ```typescript
       import type { Metadata } from 'next';
       import { siteConfig } from './site-config';

       export function createMetadata({
         title,
         description,
         path,
         ogImage,
         ...overrides
       }: {
         title: string;
         description: string;
         path: string;
         ogImage?: string;
       } & Partial<Metadata>): Metadata {
         const url = `${siteConfig.url}${path}`;
         return {
           title,
           description,
           openGraph: {
             title,
             description,
             url,
             siteName: siteConfig.name,
             type: 'website',
             images: [{ url: ogImage || siteConfig.ogImage }],
             ...overrides.openGraph,
           },
           twitter: {
             card: 'summary_large_image',
             title,
             description,
             ...overrides.twitter,
           },
           alternates: {
             canonical: url,
           },
           ...overrides,
         };
       }
       ```

    4. Create `src/types/index.ts`:
       ```typescript
       export interface NavItem {
         title: string;
         href: string;
         description?: string;
         disabled?: boolean;
         children?: NavItem[];
       }

       export interface FooterNavigation {
         tools: NavItem[];
         learn: NavItem[];
         company: NavItem[];
         legal: NavItem[];
       }
       ```

    5. Create `src/lib/navigation.ts` with the full navigation structure per RESEARCH code example:
       - `mainNavigation` array with Calculators (disabled, with children: Mortgage Calculator, Rent Affordability, Compound Interest -- all disabled), Guides (disabled), Life Stage Hubs (disabled), Compare (disabled)
       - `footerNavigation` object with four columns: tools (Calculators, Comparisons), learn (Guides, Life Stage Hubs), company (About, Editorial Standards), legal (Privacy Policy, Terms of Use, Affiliate Disclosure linking to /editorial-standards#affiliate-disclosure)
       - All nav items for future phases have `disabled: true`
       - Import NavItem and FooterNavigation types from `@/types`

    6. Replace `src/app/layout.tsx` with the root layout per RESEARCH Pattern 2 and UI-SPEC Layout Contract:
       ```typescript
       import type { Metadata } from 'next';
       import { Inter } from 'next/font/google';
       import { GoogleAnalytics } from '@next/third-parties/google';
       import { Analytics } from '@vercel/analytics/next';
       import { SpeedInsights } from '@vercel/speed-insights/next';
       import { siteConfig } from '@/lib/site-config';
       import './globals.css';

       const inter = Inter({
         subsets: ['latin'],
         variable: '--font-inter',
         display: 'swap',
       });

       export const metadata: Metadata = {
         title: {
           default: `${siteConfig.name} - ${siteConfig.tagline}`,
           template: `%s | ${siteConfig.name}`,
         },
         description: siteConfig.description,
         metadataBase: new URL(siteConfig.url),
       };

       export default function RootLayout({ children }: { children: React.ReactNode }) {
         return (
           <html lang="en" className={inter.variable}>
             <body className="min-h-screen bg-background font-sans antialiased">
               {/* Header and Footer added in Plan 02 */}
               <main>{children}</main>
               <Analytics />
               <SpeedInsights />
             </body>
             {siteConfig.gaId && <GoogleAnalytics gaId={siteConfig.gaId} />}
           </html>
         );
       }
       ```
       Note: Header/Footer components will be added in Plan 02. The layout renders GA4 conditionally based on env var presence. The `<html lang="en">` is required for accessibility. `min-h-screen bg-background font-sans antialiased` are the base body classes per UI-SPEC.

    7. Create a simple `src/app/page.tsx` placeholder (will be replaced in Plan 03):
       ```typescript
       export default function HomePage() {
         return (
           <div className="flex min-h-screen items-center justify-center">
             <h1 className="text-4xl font-semibold text-primary">WalletWaypoint</h1>
           </div>
         );
       }
       ```
  </action>
  <verify>
    <automated>cd "C:/Users/blink-llm/Documents/Development/walletwaypoint" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/globals.css contains "@import \"tailwindcss\"" and "@theme inline" and "--primary: oklch(0.35 0.1 245)" and "--accent: oklch(0.55 0.12 175)" and "--font-sans: var(--font-inter)"
    - src/app/layout.tsx contains "Inter" from "next/font/google" and "GoogleAnalytics" and "Analytics" and "SpeedInsights" and "lang=\"en\"" and "inter.variable"
    - src/lib/site-config.ts contains "name: 'WalletWaypoint'" and "NEXT_PUBLIC_GA_ID" and "NEXT_PUBLIC_SITE_URL"
    - src/lib/metadata.ts contains "export function createMetadata" and "alternates" and "canonical" and "openGraph" and "siteConfig"
    - src/lib/navigation.ts contains "export const mainNavigation" and "export const footerNavigation" and "disabled: true"
    - src/types/index.ts contains "export interface NavItem" and "export interface FooterNavigation"
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>Design system fully configured with blue-based OKLCH Tailwind theme, Inter font, analytics integration, shared config/metadata/navigation/types files, and root layout. Dev server starts and build succeeds.</done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. `npx next start` serves a page at http://localhost:3000 showing "WalletWaypoint"
3. View source of rendered HTML contains `lang="en"` on html tag
4. CSS custom properties in rendered output include `--primary: oklch(0.35 0.1 245)`
5. Font preload link for Inter is present in HTML head
</verification>

<success_criteria>
- Next.js 16 project fully scaffolded with all Phase 1 dependencies
- Tailwind CSS 4 theme configured with blue-based OKLCH palette matching UI-SPEC exactly
- shadcn/ui initialized with 10 Phase 1 components
- Root layout renders with Inter font, GA4 (conditional), Vercel Analytics, and SpeedInsights
- Shared utilities (site-config, metadata helper, navigation, types) exist and are importable
- `npx next build` succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-trust/01-01-SUMMARY.md`
</output>
