---
phase: 03-content-system-life-stage-hubs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - velite.config.ts
  - package.json
  - content/glossary.yaml
  - src/components/content/mdx-content.tsx
  - src/components/content/mdx-components.tsx
  - src/components/ui/popover.tsx
autonomous: true
requirements: [CONT-01, CONT-05]
must_haves:
  truths:
    - "Velite builds successfully with guides, hubs, and glossary collections alongside existing calculators collection"
    - "MDX content can be rendered in a client component using the useMDXComponent pattern"
    - "Glossary terms are available as a typed collection from #site/content"
    - "Popover component is installed and importable from @/components/ui/popover"
  artifacts:
    - path: "velite.config.ts"
      provides: "Extended Velite config with guides (s.mdx), hubs (YAML), and glossary (single YAML) collections plus rehype plugins"
      contains: "defineCollection"
    - path: "content/glossary.yaml"
      provides: "25 financial glossary terms with term, slug, definition, and optional longDefinition"
      contains: "terms:"
    - path: "src/components/content/mdx-content.tsx"
      provides: "Client-side MDX renderer using useMDXComponent pattern with new Function() + react/jsx-runtime"
      exports: ["MDXContent"]
    - path: "src/components/content/mdx-components.tsx"
      provides: "Shared MDX component map with HTML element overrides for guide styling"
      exports: ["mdxComponents"]
    - path: "src/components/ui/popover.tsx"
      provides: "shadcn/ui Popover component for glossary term hover definitions"
  key_links:
    - from: "velite.config.ts"
      to: "content/guides/*.mdx"
      via: "guides collection pattern"
      pattern: "pattern: 'guides/\\*\\.mdx'"
    - from: "velite.config.ts"
      to: "content/hubs/*.yaml"
      via: "hubs collection pattern"
      pattern: "pattern: 'hubs/\\*\\.yaml'"
    - from: "velite.config.ts"
      to: "content/glossary.yaml"
      via: "glossary single collection"
      pattern: "single: true"
    - from: "src/components/content/mdx-content.tsx"
      to: "src/components/content/mdx-components.tsx"
      via: "import mdxComponents"
      pattern: "import.*mdxComponents.*from.*mdx-components"
---

<objective>
Build the content infrastructure foundation: extend Velite with three new collections (guides, hubs, glossary), install rehype plugins for heading IDs, add the shadcn/ui Popover component, create the MDX rendering pipeline, and author the glossary YAML source file.

Purpose: Every content page (guides, hubs, glossary) depends on these schemas, the MDX renderer, and the glossary data. This must land first.
Output: Extended velite.config.ts, MDX rendering components, glossary.yaml with 25 terms, Popover component installed.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-system-life-stage-hubs/03-CONTEXT.md
@.planning/phases/03-content-system-life-stage-hubs/03-RESEARCH.md
@.planning/phases/03-content-system-life-stage-hubs/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From velite.config.ts (current state):
```typescript
import { defineConfig, defineCollection, s } from 'velite';
// ... existing calculator schema using s.object(), s.slug(), s.string(), s.number(), s.enum(), s.array()
const calculators = defineCollection({
  name: 'Calculator',
  pattern: 'calculators/*.yaml',
  schema: s.object({ ... }),
});
export default defineConfig({
  root: 'content',
  output: { data: '.velite', assets: 'public/static', base: '/static/', name: '[name]-[hash:6].[ext]', clean: true },
  collections: { calculators },
});
```

From src/lib/metadata.ts:
```typescript
export function createMetadata({ title, description, path, ogImage, ...overrides }: { title: string; description: string; path: string; ogImage?: string; } & Partial<Metadata>): Metadata;
```

From src/components/seo/article-schema.tsx:
```typescript
interface ArticleSchemaProps {
  title: string; description: string; url: string;
  datePublished: string; dateModified: string;
  authorName: string; authorUrl: string;
}
export function ArticleSchema(props: ArticleSchemaProps);
```

From src/components/seo/faq-schema.tsx:
```typescript
interface FaqSchemaProps { items: { question: string; answer: string }[]; }
export function FaqSchema({ items }: FaqSchemaProps);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Velite config with guides, hubs, and glossary collections + install rehype plugins</name>
  <files>velite.config.ts, package.json</files>
  <read_first>
    - velite.config.ts (current Velite config with calculator collection)
    - package.json (current dependencies)
    - content/calculators/mortgage-payment.yaml (reference YAML pattern)
  </read_first>
  <action>
1. Install rehype plugins:
   ```
   npm install rehype-slug rehype-autolink-headings
   ```

2. Install shadcn/ui Popover component:
   ```
   npx shadcn@latest add popover
   ```

3. Extend `velite.config.ts` to add three new collections BELOW the existing `calculators` collection. Keep all existing calculator schemas untouched. Add these imports at top:
   ```typescript
   import rehypeSlug from 'rehype-slug';
   import rehypeAutolinkHeadings from 'rehype-autolink-headings';
   ```

4. Add `guides` collection (per D-14, D-15, RESEARCH Pattern 1):
   ```typescript
   const guides = defineCollection({
     name: 'Guide',
     pattern: 'guides/*.mdx',
     schema: s.object({
       slug: s.slug('guides'),
       title: s.string().max(120),
       description: s.string().max(300),
       calculator: s.string(),
       hub: s.string(),
       lastUpdated: s.isodate(),
       keyTakeaways: s.array(s.string()),
       faqs: s.array(s.object({
         question: s.string(),
         answer: s.string(),
       })),
       relatedGuides: s.array(s.string()).optional(),
       metadata: s.metadata(),
       body: s.mdx(),
     }),
   });
   ```

5. Add `hubs` collection (per D-16, RESEARCH Pattern 2):
   ```typescript
   const hubs = defineCollection({
     name: 'Hub',
     pattern: 'hubs/*.yaml',
     schema: s.object({
       slug: s.slug('hubs'),
       name: s.string(),
       tagline: s.string(),
       description: s.string(),
       icon: s.string(),
       accentColor: s.string(),
       intro: s.string().optional(),
       calculatorSlugs: s.array(s.string()),
       guideSlugs: s.array(s.string()),
       tips: s.array(s.object({
         title: s.string(),
         description: s.string(),
       })),
       nextSteps: s.array(s.object({
         hubSlug: s.string(),
         label: s.string(),
       })),
     }),
   });
   ```

6. Add `glossary` collection (per D-09, RESEARCH Pattern 3):
   ```typescript
   const glossary = defineCollection({
     name: 'GlossaryTerm',
     pattern: 'glossary.yaml',
     single: true,
     schema: s.object({
       terms: s.array(s.object({
         term: s.string(),
         slug: s.slug('glossary'),
         definition: s.string().max(300),
         longDefinition: s.string().optional(),
       })),
     }),
   });
   ```

7. Update the `collections` in `defineConfig` to include all four:
   ```typescript
   collections: { calculators, guides, hubs, glossary },
   ```

8. Add `mdx` configuration to `defineConfig` for rehype plugins (per RESEARCH Pitfall 5 -- rehype-slug MUST come before rehype-autolink-headings):
   ```typescript
   mdx: {
     rehypePlugins: [
       rehypeSlug,
       [rehypeAutolinkHeadings, { behavior: 'wrap' }],
     ],
   },
   ```

The final defineConfig should look like:
```typescript
export default defineConfig({
  root: 'content',
  output: {
    data: '.velite',
    assets: 'public/static',
    base: '/static/',
    name: '[name]-[hash:6].[ext]',
    clean: true,
  },
  collections: { calculators, guides, hubs, glossary },
  mdx: {
    rehypePlugins: [
      rehypeSlug,
      [rehypeAutolinkHeadings, { behavior: 'wrap' }],
    ],
  },
});
```
  </action>
  <verify>
    <automated>cd C:/Users/blink-llm/Documents/Development/walletwaypoint && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - velite.config.ts contains `import rehypeSlug from 'rehype-slug'`
    - velite.config.ts contains `import rehypeAutolinkHeadings from 'rehype-autolink-headings'`
    - velite.config.ts contains `name: 'Guide'` and `pattern: 'guides/*.mdx'`
    - velite.config.ts contains `name: 'Hub'` and `pattern: 'hubs/*.yaml'`
    - velite.config.ts contains `name: 'GlossaryTerm'` and `single: true`
    - velite.config.ts contains `body: s.mdx()` in guides schema
    - velite.config.ts contains `metadata: s.metadata()` in guides schema
    - velite.config.ts contains `collections: { calculators, guides, hubs, glossary }`
    - velite.config.ts contains `rehypePlugins: [` with rehypeSlug before rehypeAutolinkHeadings
    - package.json contains `rehype-slug` and `rehype-autolink-headings` in dependencies
    - src/components/ui/popover.tsx exists and is importable
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Velite config has 4 collections (calculators + guides + hubs + glossary), rehype plugins installed and configured, Popover component installed</done>
</task>

<task type="auto">
  <name>Task 2: Create MDX rendering pipeline and glossary YAML data file</name>
  <files>src/components/content/mdx-content.tsx, src/components/content/mdx-components.tsx, content/glossary.yaml</files>
  <read_first>
    - velite.config.ts (after Task 1 -- confirm s.mdx() schema is in place)
    - src/components/ui/popover.tsx (confirm Popover is installed)
    - src/components/calculator/action-callout.tsx (reference for getIcon pattern)
  </read_first>
  <action>
1. Create `src/components/content/mdx-content.tsx` -- Client Component MDX renderer (per RESEARCH Pattern 4, Pitfall 1):
   ```typescript
   'use client';

   import * as runtime from 'react/jsx-runtime';
   import { mdxComponents } from './mdx-components';

   interface MDXContentProps {
     code: string;
     components?: Record<string, React.ComponentType<any>>;
   }

   function useMDXComponent(code: string) {
     const fn = new Function(code);
     return fn({ ...runtime }).default;
   }

   export function MDXContent({ code, components }: MDXContentProps) {
     const Component = useMDXComponent(code);
     return <Component components={{ ...mdxComponents, ...components }} />;
   }
   ```

2. Create `src/components/content/mdx-components.tsx` -- Shared component map with HTML element overrides (per UI-SPEC Typography, RESEARCH Pattern 4). This file will be extended in Plan 02 with custom callout components. For now, only include HTML element overrides:
   ```typescript
   import type { ComponentType } from 'react';

   // Placeholder imports -- will be replaced in Plan 02 when callout components are created
   // import { KeyTakeaway } from './key-takeaway';
   // import { ProTip } from './pro-tip';
   // import { Definition } from './definition';
   // import { Term } from './term';

   export const mdxComponents: Record<string, ComponentType<any>> = {
     // Custom MDX components will be added in Plan 02
     // Override default HTML elements for consistent guide styling
     h2: (props: any) => (
       <h2 id={props.id} className="mt-10 mb-4 text-xl font-semibold" {...props} />
     ),
     h3: (props: any) => (
       <h3 id={props.id} className="mt-8 mb-3 text-lg font-semibold" {...props} />
     ),
     p: (props: any) => (
       <p className="mb-4 text-base leading-[1.7]" {...props} />
     ),
     ul: (props: any) => (
       <ul className="mb-4 list-disc space-y-2 pl-6 text-base leading-[1.6]" {...props} />
     ),
     ol: (props: any) => (
       <ol className="mb-4 list-decimal space-y-2 pl-6 text-base leading-[1.6]" {...props} />
     ),
     a: (props: any) => (
       <a className="text-accent underline hover:text-foreground" {...props} />
     ),
     blockquote: (props: any) => (
       <blockquote className="border-l-4 border-accent/30 pl-4 italic text-muted-foreground" {...props} />
     ),
     table: (props: any) => (
       <div className="overflow-x-auto mb-4">
         <table className="w-full text-sm" {...props} />
       </div>
     ),
     th: (props: any) => (
       <th className="border-b border-border px-3 py-2 text-left font-semibold" {...props} />
     ),
     td: (props: any) => (
       <td className="border-b border-border px-3 py-2" {...props} />
     ),
   };
   ```

3. Create `content/glossary.yaml` with 25 financial terms (per D-09, RESEARCH Glossary Initial Terms). Each term has: term, slug, definition (2-3 sentences, plain English), and optional longDefinition:
   ```yaml
   terms:
     - term: APR
       slug: apr
       definition: "Annual Percentage Rate -- the total yearly cost of borrowing, including interest and fees, expressed as a percentage."
       longDefinition: "APR includes the interest rate plus any additional fees charged by the lender (origination fees, closing costs, etc.), giving you a more complete picture of what a loan actually costs compared to the interest rate alone."
     - term: DTI
       slug: dti
       definition: "Debt-to-Income ratio -- the percentage of your gross monthly income that goes toward debt payments."
       longDefinition: "Lenders use DTI to assess your ability to manage monthly payments. Most mortgage lenders prefer a DTI below 43%, with many preferring 36% or less."
     - term: Amortization
       slug: amortization
       definition: "The process of spreading a loan into a series of fixed payments over time, where each payment covers both principal and interest."
     - term: Principal
       slug: principal
       definition: "The original amount of money borrowed in a loan, not including interest."
     - term: Interest
       slug: interest
       definition: "The cost of borrowing money, usually expressed as an annual percentage of the loan balance."
     - term: Escrow
       slug: escrow
       definition: "A third-party account where funds are held during a transaction, commonly used for property taxes and insurance in mortgage payments."
     - term: PMI
       slug: pmi
       definition: "Private Mortgage Insurance -- extra insurance you pay when your down payment is less than 20% of the home price. It protects the lender, not you."
     - term: Equity
       slug: equity
       definition: "The portion of your home that you actually own -- the difference between your home's value and what you still owe on the mortgage."
     - term: Pre-approval
       slug: pre-approval
       definition: "A lender's conditional commitment to lend you a specific amount, based on a review of your credit, income, and assets. Stronger than pre-qualification."
     - term: Closing Costs
       slug: closing-costs
       definition: "Fees and expenses you pay when finalizing a real estate transaction, typically 2-5% of the home price. Includes appraisal, title insurance, and attorney fees."
     - term: Credit Score
       slug: credit-score
       definition: "A three-digit number (300-850) that represents your creditworthiness. Higher scores get better loan rates and terms."
     - term: Down Payment
       slug: down-payment
       definition: "The upfront cash you pay when purchasing a home or car, expressed as a percentage of the total price."
     - term: Fixed Rate
       slug: fixed-rate
       definition: "An interest rate that stays the same for the entire life of the loan, making your monthly payments predictable."
     - term: Variable Rate
       slug: variable-rate
       definition: "An interest rate that can change periodically based on market conditions, meaning your payments may go up or down over time."
     - term: Compound Interest
       slug: compound-interest
       definition: "Interest calculated on both the initial principal and the accumulated interest from previous periods -- your money earning money on its money."
     - term: Inflation
       slug: inflation
       definition: "The rate at which prices for goods and services increase over time, reducing the purchasing power of your money."
     - term: Net Worth
       slug: net-worth
       definition: "The total value of everything you own (assets) minus everything you owe (liabilities). A snapshot of your financial health."
     - term: Gross Income
       slug: gross-income
       definition: "Your total income before taxes and other deductions are taken out. This is the number lenders and landlords typically use."
     - term: Deduction
       slug: deduction
       definition: "An expense that reduces your taxable income. Standard deduction is a flat amount; itemized deductions are specific expenses like mortgage interest or charitable donations."
     - term: Filing Status
       slug: filing-status
       definition: "Your tax category based on marital and family status (single, married filing jointly, head of household, etc.) that determines your tax brackets and standard deduction."
     - term: Tax Bracket
       slug: tax-bracket
       definition: "The range of income taxed at a particular rate. The US uses progressive brackets, so only the income within each range is taxed at that range's rate."
     - term: Emergency Fund
       slug: emergency-fund
       definition: "Money set aside for unexpected expenses like medical bills, car repairs, or job loss. Most experts recommend 3-6 months of living expenses."
     - term: Opportunity Cost
       slug: opportunity-cost
       definition: "The potential gain you miss out on when choosing one option over another. For example, money used for a down payment could have been invested in the stock market."
     - term: Refinancing
       slug: refinancing
       definition: "Replacing an existing loan with a new one, usually to get a lower interest rate, reduce monthly payments, or change the loan term."
     - term: Index Fund
       slug: index-fund
       definition: "A type of investment fund that tracks a market index like the S&P 500, offering broad diversification at low cost. A popular choice for long-term investing."
   ```
  </action>
  <verify>
    <automated>cd C:/Users/blink-llm/Documents/Development/walletwaypoint && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/content/mdx-content.tsx exists and contains `'use client'` directive
    - src/components/content/mdx-content.tsx contains `export function MDXContent`
    - src/components/content/mdx-content.tsx contains `new Function(code)`
    - src/components/content/mdx-content.tsx contains `import * as runtime from 'react/jsx-runtime'`
    - src/components/content/mdx-components.tsx contains `export const mdxComponents`
    - src/components/content/mdx-components.tsx contains `h2:` and `h3:` and `p:` element overrides
    - src/components/content/mdx-components.tsx contains `leading-[1.7]` in paragraph override (guide body line height)
    - content/glossary.yaml contains `terms:` at root level
    - content/glossary.yaml contains at least 25 term entries (grep count of `- term:` >= 25)
    - content/glossary.yaml contains entries for APR, DTI, Amortization, Principal, Compound Interest
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>MDX rendering pipeline (mdx-content.tsx + mdx-components.tsx) created with HTML element overrides for guide typography. Glossary YAML with 25 financial terms authored and ready for Velite processing.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- velite.config.ts has 4 collections: calculators, guides, hubs, glossary
- content/glossary.yaml has 25 terms
- MDXContent component exports from src/components/content/mdx-content.tsx
- Popover component exists at src/components/ui/popover.tsx
- rehype-slug and rehype-autolink-headings are in package.json dependencies
</verification>

<success_criteria>
- Velite config extended with guides (MDX), hubs (YAML), glossary (single YAML) collections
- rehype-slug and rehype-autolink-headings installed and configured in Velite mdx options
- MDX renderer (useMDXComponent pattern) created as client component
- mdxComponents map has HTML element overrides matching UI-SPEC typography
- Popover component installed via shadcn CLI
- Glossary YAML contains 25 financial terms with plain-English definitions
- All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-system-life-stage-hubs/03-01-SUMMARY.md`
</output>
