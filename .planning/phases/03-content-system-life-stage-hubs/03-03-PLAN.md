---
phase: 03-content-system-life-stage-hubs
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/content/hub-hero.tsx
  - src/components/content/hub-section.tsx
  - src/components/content/quick-tips.tsx
  - src/components/content/next-steps.tsx
  - src/app/hubs/[slug]/page.tsx
  - src/app/hubs/page.tsx
  - src/app/hubs/layout.tsx
  - content/hubs/student-new-grad.yaml
  - content/hubs/first-time-renter.yaml
  - content/hubs/first-home-buyer.yaml
  - content/hubs/rent-vs-buy.yaml
  - content/hubs/freelancer.yaml
  - content/hubs/pre-retirement.yaml
autonomous: true
requirements: [HUB-01, HUB-02, HUB-03, HUB-04, HUB-05, HUB-06]
must_haves:
  truths:
    - "All 6 life-stage hub pages render with unique icon and accent color"
    - "Hub pages aggregate relevant calculators and guides into curated sections"
    - "Hub pages display Quick Tips checklist and Next Steps links to other hubs"
    - "Hub index page shows all 6 hubs in a card grid"
    - "Each hub links to real calculator and guide pages"
  artifacts:
    - path: "src/components/content/hub-hero.tsx"
      provides: "Hub hero banner with icon, name, tagline, accent color (D-05, D-08)"
      exports: ["HubHero"]
    - path: "src/components/content/hub-section.tsx"
      provides: "Hub curated section rendering calculator or guide cards (D-05)"
      exports: ["HubSection"]
    - path: "src/components/content/quick-tips.tsx"
      provides: "Hub tips checklist section (D-05)"
      exports: ["QuickTips"]
    - path: "src/components/content/next-steps.tsx"
      provides: "Related hub links with navigation (D-05)"
      exports: ["NextSteps"]
    - path: "src/app/hubs/[slug]/page.tsx"
      provides: "Dynamic hub page route with SSG via generateStaticParams"
      exports: ["default", "generateStaticParams", "generateMetadata"]
    - path: "src/app/hubs/page.tsx"
      provides: "Hub index page with card grid showing all 6 hubs"
    - path: "content/hubs/student-new-grad.yaml"
      provides: "Student/New Grad hub YAML config (HUB-01)"
      contains: "slug: student-new-grad"
    - path: "content/hubs/first-time-renter.yaml"
      provides: "First-Time Renter hub YAML config (HUB-02)"
      contains: "slug: first-time-renter"
    - path: "content/hubs/first-home-buyer.yaml"
      provides: "First Home Buyer hub YAML config (HUB-03)"
      contains: "slug: first-home-buyer"
    - path: "content/hubs/rent-vs-buy.yaml"
      provides: "Rent vs Buy hub YAML config (HUB-04)"
      contains: "slug: rent-vs-buy"
    - path: "content/hubs/freelancer.yaml"
      provides: "Freelancer hub YAML config (HUB-05)"
      contains: "slug: freelancer"
    - path: "content/hubs/pre-retirement.yaml"
      provides: "Pre-Retirement hub YAML config (HUB-06)"
      contains: "slug: pre-retirement"
  key_links:
    - from: "src/app/hubs/[slug]/page.tsx"
      to: "#site/content"
      via: "import { hubs, calculators, guides } from '#site/content'"
      pattern: "import.*hubs.*from.*#site/content"
    - from: "content/hubs/*.yaml"
      to: "content/calculators/*.yaml"
      via: "calculatorSlugs references"
      pattern: "calculatorSlugs:"
    - from: "content/hubs/*.yaml"
      to: "content/guides/*.mdx"
      via: "guideSlugs references"
      pattern: "guideSlugs:"
---

<objective>
Build the hub page system: 4 hub-specific components (hero, section, quick tips, next steps), the hub [slug] route with SSG, the hub index page, a hubs layout with DisclaimerBanner, and all 6 hub YAML configuration files.

Purpose: Life-stage hubs aggregate calculators and guides into curated experiences for specific user segments (student, renter, homebuyer, etc.). This plan creates the complete hub rendering system.
Output: 4 new components, hub [slug] route, hub index page, hub layout, 6 hub YAML configs in content/hubs/.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-content-system-life-stage-hubs/03-CONTEXT.md
@.planning/phases/03-content-system-life-stage-hubs/03-RESEARCH.md
@.planning/phases/03-content-system-life-stage-hubs/03-UI-SPEC.md
@.planning/phases/03-content-system-life-stage-hubs/03-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 and existing codebase -->

From velite.config.ts (hub schema -- created in Plan 01):
```typescript
// Hub collection schema produces objects with:
// slug, name, tagline, description, icon (Lucide icon name string), accentColor (OKLCH string),
// intro (optional string), calculatorSlugs (string[]), guideSlugs (string[]),
// tips ({ title: string, description: string }[]),
// nextSteps ({ hubSlug: string, label: string }[])
```

From velite.config.ts (calculator schema -- existing):
```typescript
// Calculator objects have: slug, title, description, category, inputs, charts, outputs, seo, callouts, interpretation, mathModule
```

From velite.config.ts (guide schema -- created in Plan 01):
```typescript
// Guide objects have: slug, title, description, calculator, hub, lastUpdated, keyTakeaways[], faqs[], relatedGuides[], metadata (readingTime, wordCount), body
```

From src/components/calculator/action-callout.tsx (getIcon pattern):
```typescript
import * as LucideIcons from 'lucide-react';
import type { LucideIcon } from 'lucide-react';
function getIcon(iconName?: string): LucideIcon {
  if (!iconName) return ArrowRight;
  const Icon = (LucideIcons as unknown as Record<string, LucideIcon>)[iconName];
  return Icon || ArrowRight;
}
```

From src/lib/metadata.ts:
```typescript
export function createMetadata({ title, description, path }: { ... }): Metadata;
```

From src/components/trust/disclaimer-banner.tsx (existing, used in guides layout):
```typescript
export function DisclaimerBanner(): JSX.Element;
```

Existing components available:
- Card, CardContent, CardHeader, CardTitle, CardDescription, CardFooter from '@/components/ui/card'
- Badge from '@/components/ui/badge'
- Button from '@/components/ui/button' (render prop pattern: `<Button nativeButton={false} render={<Link href="..." />}>`)
- Separator from '@/components/ui/separator'

Hub-to-Content Mapping (from RESEARCH.md):
| Hub Slug | Calculators | Guides | Icon | Accent Color |
|----------|-------------|--------|------|--------------|
| student-new-grad | student-loan, budget, savings-goal | student-loan, budget, savings-goal | GraduationCap | oklch(0.55 0.12 145) |
| first-time-renter | rent-affordability, budget | rent-affordability, budget | Building | oklch(0.55 0.1 200) |
| first-home-buyer | mortgage-payment, savings-goal, rent-vs-buy | mortgage-payment, savings-goal, rent-vs-buy | Home | oklch(0.55 0.12 175) |
| rent-vs-buy | rent-vs-buy, mortgage-payment, compound-interest | rent-vs-buy, mortgage-payment, compound-interest | Scale | oklch(0.50 0.1 260) |
| freelancer | tax-estimator, budget, savings-goal, retirement | tax-estimator, budget, savings-goal, retirement | Briefcase | oklch(0.55 0.12 45) |
| pre-retirement | retirement, compound-interest, savings-goal | retirement, compound-interest, savings-goal | Landmark | oklch(0.40 0.1 245) |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hub components (hero, section, quick tips, next steps) and hub layout</name>
  <files>
    src/components/content/hub-hero.tsx,
    src/components/content/hub-section.tsx,
    src/components/content/quick-tips.tsx,
    src/components/content/next-steps.tsx,
    src/app/hubs/layout.tsx
  </files>
  <read_first>
    - src/components/calculator/action-callout.tsx (getIcon pattern for dynamic Lucide icons)
    - src/components/ui/card.tsx (Card component API)
    - src/components/ui/button.tsx (Button component with render prop pattern)
    - src/app/guides/layout.tsx (reference for layout with DisclaimerBanner)
    - src/components/trust/disclaimer-banner.tsx (DisclaimerBanner component)
  </read_first>
  <action>
1. Create `src/components/content/hub-hero.tsx` (per D-05, D-08, UI-SPEC Hub Page Layout):
   - Client component (`'use client'`) because it uses the dynamic getIcon pattern with LucideIcons import
   - Props: `{ name: string; tagline: string; icon: string; accentColor: string; intro?: string }`
   - Layout: full-width section with `bg-[color:var(--hub-accent)]/5` (5% opacity tinted background)
   - Container: `<div style={{ '--hub-accent': accentColor } as React.CSSProperties}>`
   - Inner: `max-w-[1200px] mx-auto py-12 md:py-16 px-4 sm:px-6`
   - Icon: use getIcon pattern (copy from action-callout.tsx): `import * as LucideIcons from 'lucide-react'` etc. Render at `size={48}` on desktop, `size={32}` on mobile, in `text-[color:var(--hub-accent)]`
   - H1: `text-2xl font-semibold text-foreground` (hub name)
   - Tagline: `text-base text-muted-foreground mt-2`
   - Intro paragraph (if provided): `text-base text-foreground mt-4 max-w-[640px]`
   - Use `<section>` wrapper with `aria-label={name + " hub"}`

2. Create `src/components/content/hub-section.tsx` (per D-05, D-07, UI-SPEC):
   - NOT a client component (no interactivity -- pure rendering)
   - Props:
     ```typescript
     interface HubSectionProps {
       title: string;
       intro: string;
       children: React.ReactNode;
     }
     ```
   - Layout: `<section className="py-12" aria-label={title}>`
   - Heading: `<h2 className="text-xl font-semibold text-foreground">{title}</h2>`
   - Intro: `<p className="text-sm text-muted-foreground mt-1 mb-6">{intro}</p>`
   - Children slot for content grid (caller renders the cards)

3. Create `src/components/content/quick-tips.tsx` (per D-05, UI-SPEC):
   - NOT a client component
   - Props: `{ tips: { title: string; description: string }[] }`
   - Container: `<section className="py-12 bg-muted rounded-xl px-6" aria-label="Quick Tips">`
   - Heading: `<h2 className="text-xl font-semibold text-foreground">Quick Tips</h2>`
   - Intro: `<p className="text-sm text-muted-foreground mt-1 mb-6">Practical advice to keep in mind.</p>`
   - Grid: `grid grid-cols-1 md:grid-cols-2 gap-4`
   - Each tip: flex row with a CheckCircle2 icon (from lucide-react, `text-chart-3` green, size 5) + div with title (text-sm font-semibold) and description (text-sm text-muted-foreground)
   - Import `CheckCircle2` from `lucide-react`

4. Create `src/components/content/next-steps.tsx` (per D-05, UI-SPEC):
   - Client component (`'use client'`) because it uses getIcon pattern for hub icons
   - Props: `{ steps: { hubSlug: string; label: string }[]; hubs: Array<{ slug: string; icon: string; accentColor: string }> }`
   - The parent hub page resolves hub data and passes it as props
   - Layout: `<section className="py-12" aria-label="Next Steps">`
   - Heading: `<h2 className="text-xl font-semibold text-foreground">Next Steps</h2>`
   - Intro: `<p className="text-sm text-muted-foreground mt-1 mb-6">Ready to explore more? These hubs might be your next stop.</p>`
   - Cards: `flex flex-col sm:flex-row gap-4`
   - Each card: Link to `/hubs/${step.hubSlug}` wrapping a Card component
     - Content: hub icon (from matching hub data, rendered in that hub's accent color) + step.label text + ArrowRight icon
     - Hover: `hover:border-[color:var(--step-accent)]/50 transition-colors`
     - Set `--step-accent` via inline style from the matched hub's accentColor

5. Create `src/app/hubs/layout.tsx` (similar to guides layout):
   - Import DisclaimerBanner from `@/components/trust/disclaimer-banner`
   - Render `<DisclaimerBanner />` before `{children}`
   - This ensures financial disclaimers appear on all hub pages (per RESEARCH project constraints)
  </action>
  <verify>
    <automated>cd C:/Users/blink-llm/Documents/Development/walletwaypoint && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/content/hub-hero.tsx contains `'use client'` and `--hub-accent` CSS variable
    - src/components/content/hub-hero.tsx contains `aria-label` on section element
    - src/components/content/hub-hero.tsx contains `max-w-[1200px]` container
    - src/components/content/hub-section.tsx contains `aria-label={title}` on section
    - src/components/content/quick-tips.tsx contains `bg-muted rounded-xl` container
    - src/components/content/quick-tips.tsx contains `CheckCircle2` icon import
    - src/components/content/quick-tips.tsx contains "Quick Tips" heading
    - src/components/content/next-steps.tsx contains `'use client'` and ArrowRight icon
    - src/components/content/next-steps.tsx contains `Next Steps` heading
    - src/app/hubs/layout.tsx contains `DisclaimerBanner`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All 4 hub components created with correct styling, accent color system, and accessibility. Hub layout with DisclaimerBanner created.</done>
</task>

<task type="auto">
  <name>Task 2: Create hub [slug] route, hub index page, and all 6 hub YAML configs</name>
  <files>
    src/app/hubs/[slug]/page.tsx,
    src/app/hubs/page.tsx,
    content/hubs/student-new-grad.yaml,
    content/hubs/first-time-renter.yaml,
    content/hubs/first-home-buyer.yaml,
    content/hubs/rent-vs-buy.yaml,
    content/hubs/freelancer.yaml,
    content/hubs/pre-retirement.yaml
  </files>
  <read_first>
    - src/app/calculators/[slug]/page.tsx (reference SSG pattern)
    - src/app/hubs/page.tsx (existing placeholder to replace)
    - src/components/content/hub-hero.tsx (HubHero component from Task 1)
    - src/components/content/hub-section.tsx (HubSection component from Task 1)
    - src/components/content/quick-tips.tsx (QuickTips component from Task 1)
    - src/components/content/next-steps.tsx (NextSteps component from Task 1)
    - src/lib/metadata.ts (createMetadata helper)
    - src/lib/site-config.ts (siteConfig values)
    - velite.config.ts (hub schema definition)
  </read_first>
  <action>
1. Create `src/app/hubs/[slug]/page.tsx` (per RESEARCH Pattern 7, UI-SPEC Hub Page Layout):
   - Import: `hubs`, `calculators`, `guides` from `#site/content`, all hub components, createMetadata, siteConfig, notFound, Link, Card components, Badge
   - `generateStaticParams`: return `hubs.map((h) => ({ slug: h.slug }))`
   - `generateMetadata`: find hub by slug, return createMetadata with title: hub.name, description: hub.description, path: "/hubs/" + hub.slug
   - Default export `HubPage` (async Server Component):
     a. Await params, find hub by slug, notFound() if not found
     b. Resolve calculator and guide objects from slugs:
        ```typescript
        const hubCalculators = calculators.filter((c) => hub.calculatorSlugs.includes(c.slug));
        const hubGuides = guides.filter((g) => hub.guideSlugs.includes(g.slug));
        const nextStepHubs = hubs.filter((h) => hub.nextSteps.some((ns) => ns.hubSlug === h.slug));
        ```
     c. Render (per UI-SPEC Layout Contract):
        ```
        <HubHero name={hub.name} tagline={hub.tagline} icon={hub.icon} accentColor={hub.accentColor} intro={hub.intro} />
        <div className="max-w-[1200px] mx-auto px-4 sm:px-6">
          {/* Your Calculators section */}
          <HubSection title="Your Calculators" intro="Interactive tools to run the numbers for your situation.">
            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
              {hubCalculators.map((calc) => (
                // Calculator card: Link wrapping Card with title, description, "Try this calculator" link
                // Each card links to /calculators/{calc.slug}
              ))}
            </div>
            {hubCalculators.length === 0 && (
              <p className="text-sm text-muted-foreground">Calculators for this life stage are coming soon. Explore our other <Link href="/calculators">calculators</Link> in the meantime.</p>
            )}
          </HubSection>

          {/* Guides For You section */}
          <HubSection title="Guides For You" intro="Clear explanations to help you understand the concepts.">
            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
              {hubGuides.map((guide) => (
                // Guide card: Link wrapping Card with title, description, reading time badge, "Read guide" link
                // Each card links to /guides/{guide.slug}
              ))}
            </div>
            {hubGuides.length === 0 && (
              <p className="text-sm text-muted-foreground">Guides for this life stage are coming soon. Check out our other <Link href="/guides">guides</Link> in the meantime.</p>
            )}
          </HubSection>

          {/* Quick Tips section */}
          <QuickTips tips={hub.tips} />

          {/* Next Steps section */}
          <NextSteps steps={hub.nextSteps} hubs={nextStepHubs} />
        </div>
        ```
     d. Calculator cards: Card with CardContent containing icon (use getIcon pattern with calc category-based icon or generic Calculator icon) + CardTitle (calc.title) + CardDescription (calc.description, 2-line clamp) + "Try this calculator" link text (text-sm text-accent)
     e. Guide cards: Card with CardContent containing CardTitle (guide.title) + CardDescription (guide.description, 2-line clamp) + Badge showing `{guide.metadata.readingTime} min read` + "Read guide" link text (text-sm text-accent)
     f. Card hover: `hover:border-accent/50 transition-colors` (per UI-SPEC Interaction Contract)

2. Replace `src/app/hubs/page.tsx` placeholder with real hub index page:
   - Import `hubs` from `#site/content`, createMetadata, Link, Card, Badge
   - Client component needed for hub icon rendering -- OR use the getIcon pattern inline (client component)
   - Actually: make this a Server Component and use a small HubIconCard client component within it, OR simply render without icons (use text only) and keep it as Server Component. Simplest: make the page itself use dynamic icon import.
   - Approach: Create the page as a Server Component. For hub icons in the index cards, use `import * as LucideIcons from 'lucide-react'` in a small client wrapper, OR just skip icons on the index page and use the accent color as a visual indicator instead.
   - Best approach: Create a small inline `HubCard` client component at the bottom of the file (or in a separate file) that renders the icon.
   - Export `metadata` using createMetadata: title "Life Stage Hubs", description "Financial tools and guides organized by where you are in life. Find your stage, get your roadmap.", path "/hubs"
   - Page layout (max-w-[1200px] mx-auto px-4 sm:px-6 py-12 md:py-16):
     - H1: "Life Stage Hubs" (text-2xl font-semibold)
     - Description: "Financial tools and guides organized by where you are in life. Find your stage, get your roadmap." (text-base text-muted-foreground mt-3 max-w-[640px])
     - Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 mt-8`
     - Each hub card: Link wrapping Card with hub icon (in accent color), hub name, tagline, "Explore this hub" CTA
     - Card hover uses hub accent color: set `--hub-accent` via inline style, `hover:border-[color:var(--hub-accent)]/50 transition-colors`

3. Create all 6 hub YAML config files in `content/hubs/`. Each file follows this exact structure from the RESEARCH.md Hub YAML Config Format:

   **content/hubs/student-new-grad.yaml:**
   ```yaml
   slug: student-new-grad
   name: "Student / New Grad"
   tagline: "Your post-graduation money roadmap"
   description: "Just graduated? Here's everything you need to tackle student loans, build your first budget, and start saving -- even on an entry-level salary."
   icon: GraduationCap
   accentColor: "oklch(0.55 0.12 145)"
   intro: "Just graduated? Congrats -- and welcome to the real world of money. Whether you are staring down student loans, figuring out your first paycheck, or wondering how to start saving, this hub has the tools and guides to help you build a solid financial foundation from day one."

   calculatorSlugs:
     - student-loan
     - budget
     - savings-goal

   guideSlugs:
     - student-loan
     - budget
     - savings-goal

   tips:
     - title: "Start with your student loans"
       description: "Know your repayment options before your grace period ends. Income-driven plans can make payments manageable."
     - title: "Build a simple budget"
       description: "The 50/30/20 rule is a great starting point: 50% needs, 30% wants, 20% savings and debt repayment."
     - title: "Open a high-yield savings account"
       description: "Even $50/month adds up. Start building an emergency fund before you need one."
     - title: "Take the employer 401(k) match"
       description: "If your employer matches retirement contributions, that's free money. Contribute at least enough to get the full match."

   nextSteps:
     - hubSlug: first-time-renter
       label: "Moving out? Renting guide"
     - hubSlug: freelancer
       label: "Going freelance? Tax and budget tools"
   ```

   **content/hubs/first-time-renter.yaml:**
   ```yaml
   slug: first-time-renter
   name: "First-Time Renter"
   tagline: "Smart renting starts with knowing your numbers"
   description: "Find out how much rent you can really afford, build a budget that works, and understand what to expect before signing your first lease."
   icon: Building
   accentColor: "oklch(0.55 0.1 200)"
   intro: "Apartment hunting is exciting -- until you see the prices. This hub helps you figure out what you can actually afford, build a budget around your rent, and avoid the common first-renter mistakes that cost real money."

   calculatorSlugs:
     - rent-affordability
     - budget

   guideSlugs:
     - rent-affordability
     - budget

   tips:
     - title: "Follow the 30% rule (with caveats)"
       description: "Spending no more than 30% of gross income on rent is a guideline, not a law. In expensive cities, adjust your other spending categories."
     - title: "Budget for move-in costs"
       description: "First month, last month, security deposit, and moving expenses can add up to 3-4 months of rent upfront."
     - title: "Read the entire lease"
       description: "Understand your obligations: early termination fees, maintenance responsibilities, and renewal terms before you sign."
     - title: "Get renter's insurance"
       description: "It's typically $15-30/month and covers your belongings against theft, fire, and water damage. Your landlord's insurance doesn't cover your stuff."

   nextSteps:
     - hubSlug: first-home-buyer
       label: "Ready to buy? Homebuyer guide"
     - hubSlug: student-new-grad
       label: "Still paying off loans? Student hub"
   ```

   **content/hubs/first-home-buyer.yaml:**
   ```yaml
   slug: first-home-buyer
   name: "First Home Buyer"
   tagline: "Your roadmap from dreaming to closing day"
   description: "Everything you need to understand mortgages, save for a down payment, and make your first home purchase with confidence."
   icon: Home
   accentColor: "oklch(0.55 0.12 175)"
   intro: "Buying your first home is one of the biggest financial decisions you will ever make. This hub walks you through the mortgage math, helps you figure out how much to save, and gives you the tools to compare renting versus buying -- so you can make the call with confidence, not anxiety."

   calculatorSlugs:
     - mortgage-payment
     - savings-goal
     - rent-vs-buy

   guideSlugs:
     - mortgage-payment
     - savings-goal
     - rent-vs-buy

   tips:
     - title: "Get pre-approved before house hunting"
       description: "A pre-approval letter shows sellers you're serious and helps you know your real budget."
     - title: "Budget for closing costs"
       description: "Expect 2-5% of the home price in closing costs on top of your down payment."
     - title: "Don't open new credit lines before closing"
       description: "New credit inquiries can lower your score and jeopardize your mortgage approval."
     - title: "Consider total cost of ownership"
       description: "Your mortgage payment is just the start. Factor in property taxes, insurance, maintenance (1-2% of home value/year), and HOA fees."

   nextSteps:
     - hubSlug: first-time-renter
       label: "Still renting? Start here"
     - hubSlug: freelancer
       label: "Self-employed? Special mortgage considerations"
   ```

   **content/hubs/rent-vs-buy.yaml:**
   ```yaml
   slug: rent-vs-buy
   name: "Rent vs Buy"
   tagline: "The real math behind the biggest financial decision"
   description: "Should you keep renting or take the leap into homeownership? Compare the true costs, factor in opportunity cost, and make the call based on your numbers."
   icon: Scale
   accentColor: "oklch(0.50 0.1 260)"
   intro: "Everyone has an opinion on renting versus buying. We have the math. This hub gives you the calculators and guides to compare the real costs -- including the opportunity cost of your down payment, closing costs, and the break-even timeline. The right answer depends on your situation, not conventional wisdom."

   calculatorSlugs:
     - rent-vs-buy
     - mortgage-payment
     - compound-interest

   guideSlugs:
     - rent-vs-buy
     - mortgage-payment
     - compound-interest

   tips:
     - title: "Calculate your break-even point"
       description: "The rent-vs-buy calculation changes dramatically based on how long you plan to stay. Buying typically wins after 5-7 years."
     - title: "Don't forget opportunity cost"
       description: "Money locked in a down payment could be invested elsewhere. Our calculator factors this in."
     - title: "Factor in lifestyle flexibility"
       description: "Renting offers mobility. If your career or life plans might shift in the next few years, that flexibility has real value."
     - title: "Consider your local market"
       description: "Price-to-rent ratios vary wildly by city. In some markets renting is clearly better; in others, buying wins easily."

   nextSteps:
     - hubSlug: first-home-buyer
       label: "Leaning toward buying? Start here"
     - hubSlug: first-time-renter
       label: "Prefer renting? Optimize your rent budget"
   ```

   **content/hubs/freelancer.yaml:**
   ```yaml
   slug: freelancer
   name: "Freelancer / Self-Employed"
   tagline: "Master your taxes, budget, and savings on your own terms"
   description: "When you are your own boss, you are also your own HR, accountant, and financial planner. Get the tools to handle taxes, budgeting, savings, and retirement without an employer safety net."
   icon: Briefcase
   accentColor: "oklch(0.55 0.12 45)"
   intro: "No employer match. No automatic tax withholding. No benefits package. When you work for yourself, you handle it all -- but that also means you get to optimize it all. This hub gives you the calculators and guides to estimate your taxes, build a freelancer-friendly budget, save for retirement, and plan for the income ups and downs."

   calculatorSlugs:
     - tax-estimator
     - budget
     - savings-goal
     - retirement

   guideSlugs:
     - tax-estimator
     - budget
     - savings-goal
     - retirement

   tips:
     - title: "Pay quarterly estimated taxes"
       description: "The IRS expects self-employed people to pay taxes four times a year (April, June, September, January). Underpaying triggers penalties."
     - title: "Separate business and personal finances"
       description: "Open a dedicated business checking account. It makes tax time dramatically easier and protects you legally."
     - title: "Save 25-30% of income for taxes"
       description: "Self-employment tax (15.3%) plus income tax adds up fast. Set aside money from every payment you receive."
     - title: "Build a bigger emergency fund"
       description: "Without a steady paycheck, aim for 6-12 months of expenses (not the standard 3-6) to weather dry spells."

   nextSteps:
     - hubSlug: pre-retirement
       label: "Planning ahead? Retirement tools"
     - hubSlug: student-new-grad
       label: "Just starting out? Financial basics"
   ```

   **content/hubs/pre-retirement.yaml:**
   ```yaml
   slug: pre-retirement
   name: "Pre-Retirement"
   tagline: "Make your money last as long as you do"
   description: "Approaching retirement? Estimate how much you need, understand withdrawal strategies, and plan for the transition from saving to spending."
   icon: Landmark
   accentColor: "oklch(0.40 0.1 245)"
   intro: "You have spent decades saving. Now the question shifts from 'How much should I save?' to 'How much can I spend?' This hub helps you estimate your retirement needs, understand how compound interest works in your favor (and against you through inflation), and build a savings strategy for the final stretch."

   calculatorSlugs:
     - retirement
     - compound-interest
     - savings-goal

   guideSlugs:
     - retirement
     - compound-interest
     - savings-goal

   tips:
     - title: "Know your number"
       description: "Most financial planners suggest you will need 70-80% of your pre-retirement income annually. Use our calculator to get your specific target."
     - title: "Understand the 4% rule"
       description: "The classic guideline suggests withdrawing 4% of your portfolio in year one, then adjusting for inflation. It is a starting point, not a guarantee."
     - title: "Delay Social Security if you can"
       description: "Benefits increase roughly 8% for each year you delay past 62, up to age 70. That's a guaranteed return hard to beat."
     - title: "Plan for healthcare costs"
       description: "Medicare starts at 65, but it does not cover everything. Budget for supplemental insurance, dental, vision, and potential long-term care."

   nextSteps:
     - hubSlug: freelancer
       label: "Self-employed? Retirement planning is different"
     - hubSlug: rent-vs-buy
       label: "Considering downsizing? Rent vs buy analysis"
   ```
  </action>
  <verify>
    <automated>cd C:/Users/blink-llm/Documents/Development/walletwaypoint && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/hubs/[slug]/page.tsx contains `generateStaticParams` returning hubs.map
    - src/app/hubs/[slug]/page.tsx contains `import { hubs, calculators, guides } from '#site/content'`
    - src/app/hubs/[slug]/page.tsx contains `<HubHero` and `<HubSection` and `<QuickTips` and `<NextSteps`
    - src/app/hubs/[slug]/page.tsx contains `max-w-[1200px]` container
    - src/app/hubs/[slug]/page.tsx contains `lg:grid-cols-3` for card grids
    - src/app/hubs/page.tsx no longer contains "Coming Soon"
    - src/app/hubs/page.tsx contains `import { hubs } from '#site/content'`
    - src/app/hubs/page.tsx contains "Life Stage Hubs" as H1 text
    - src/app/hubs/page.tsx contains "Explore this hub" CTA text
    - content/hubs/student-new-grad.yaml exists and contains `slug: student-new-grad` and `icon: GraduationCap`
    - content/hubs/first-time-renter.yaml exists and contains `slug: first-time-renter` and `icon: Building`
    - content/hubs/first-home-buyer.yaml exists and contains `slug: first-home-buyer` and `icon: Home`
    - content/hubs/rent-vs-buy.yaml exists and contains `slug: rent-vs-buy` and `icon: Scale`
    - content/hubs/freelancer.yaml exists and contains `slug: freelancer` and `icon: Briefcase`
    - content/hubs/pre-retirement.yaml exists and contains `slug: pre-retirement` and `icon: Landmark`
    - All 6 YAML files contain `calculatorSlugs:` and `guideSlugs:` and `tips:` and `nextSteps:`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Hub [slug] route renders all 6 hubs with hero, calculator cards, guide cards, quick tips, and next steps. Hub index page shows all hubs in card grid with accent colors. All 6 hub YAML configs authored with correct calculator/guide slug references.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- 4 new hub components exist in src/components/content/
- Hub [slug] route has generateStaticParams, generateMetadata
- Hub index page has card grid with dynamic hub data
- 6 hub YAML files exist in content/hubs/ with correct slugs, icons, accent colors
- Each hub YAML references valid calculator and guide slugs
</verification>

<success_criteria>
- All 6 hub pages render with unique icon and accent color from YAML config
- Hub pages show curated calculator cards, guide cards, quick tips, and next steps
- Hub index page shows all 6 hubs in responsive card grid
- Empty state messages display when no calculators/guides are found
- Hub layout includes DisclaimerBanner
- All hub YAML files validate against Velite schema
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-system-life-stage-hubs/03-03-SUMMARY.md`
</output>
