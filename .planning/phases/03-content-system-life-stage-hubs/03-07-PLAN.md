---
phase: 03-content-system-life-stage-hubs
plan: 07
type: execute
wave: 5
depends_on: [03-06]
files_modified: []
autonomous: false
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05, HUB-01, HUB-02, HUB-03, HUB-04, HUB-05, HUB-06]
must_haves:
  truths:
    - "All 10 guide pages render correctly with MDX content, callout boxes, TOC, and CTA banner"
    - "All 6 hub pages render with correct calculator/guide cards, tips, and next steps"
    - "Glossary terms show popover definitions on hover/tap throughout guide content"
    - "Glossary page search filters terms correctly"
    - "Navigation links work for Guides, Hubs, and Glossary"
    - "Site builds and runs without errors"
  artifacts: []
  key_links: []
---

<objective>
Final verification of the complete Phase 3 content system. Run the development server, verify all pages render correctly, and get human sign-off on content quality and visual appearance.

Purpose: Catch any rendering issues, broken links, or content problems before marking Phase 3 complete.
Output: Verified Phase 3 with human approval.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-content-system-life-stage-hubs/03-CONTEXT.md
@.planning/phases/03-content-system-life-stage-hubs/03-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification -- compile TypeScript, run Velite, start dev server</name>
  <files></files>
  <read_first>
    - velite.config.ts (verify 4 collections defined)
    - package.json (verify all dependencies present)
  </read_first>
  <action>
1. Run TypeScript compilation check:
   ```
   npx tsc --noEmit
   ```

2. Run Velite build to verify all content processes:
   ```
   npx velite build
   ```
   Confirm output shows all 10 guides, 6 hubs, and glossary processed without errors.

3. Start the Next.js dev server:
   ```
   npm run dev
   ```

4. Verify key pages load without errors by checking the dev server output for compilation errors.

5. Verify the following URLs are accessible (check for 200 status):
   - /guides (index page with 10 guide cards)
   - /guides/mortgage-payment (first guide page)
   - /guides/student-loan (last guide page)
   - /hubs (index page with 6 hub cards)
   - /hubs/student-new-grad (first hub)
   - /hubs/pre-retirement (last hub)
   - /glossary (glossary page with 25 terms)

6. Check that the sitemap includes new URLs:
   - Visit /sitemap.xml and verify guide and hub URLs are present

7. If any build errors occur, fix them before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd C:/Users/blink-llm/Documents/Development/walletwaypoint && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compiles with 0 errors
    - Velite build completes without errors
    - Dev server starts without compilation errors
    - /guides page loads with guide cards
    - /hubs page loads with hub cards
    - /glossary page loads with term listings
  </acceptance_criteria>
  <done>Full build pipeline verified: TypeScript, Velite, and Next.js dev server all succeed. All content pages are accessible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 3 content system</name>
  <action>
Human verifies the complete Phase 3 content system. What was built:
- 10 educational guide pages (2000-3000 words each) with MDX callout boxes, sticky TOC, calculator CTA banners, and FAQ sections
- 6 life-stage hub pages with curated calculator/guide cards, quick tips, and next steps navigation
- Standalone glossary page with 25 financial terms, alphabetical listing, A-Z jump links, and search filter
- Glossary term hover popovers throughout guide content
- Updated sitemap and navigation with all new content URLs enabled

Verification steps:
1. Start dev server if not running: npm run dev
2. Visit http://localhost:3000/guides -- verify 10 guide cards displayed in grid
3. Click into "How Mortgage Payments Actually Work" guide -- verify two-column layout, callout boxes (blue KeyTakeaway, green ProTip), glossary term popovers, sticky TOC, FAQ accordion, related guides
4. Visit http://localhost:3000/hubs -- verify 6 hub cards with unique icons and accent colors
5. Click into "First Home Buyer" hub -- verify hero banner, calculator cards, guide cards, quick tips, next steps
6. Visit http://localhost:3000/glossary -- verify alphabetical listing, search filtering, A-Z navigation
7. Check header navigation -- Guides and Hubs links are clickable
8. Verify content tone -- read a few paragraphs and confirm "smart friend" conversational tone
  </action>
  <verify>
    <automated>echo "Human verification required -- no automated check"</automated>
  </verify>
  <acceptance_criteria>
    - Human approves guide page rendering (callout boxes, TOC, CTA banner visible)
    - Human approves hub page rendering (correct cards, tips, next steps)
    - Human approves glossary page (search works, terms listed alphabetically)
    - Human approves navigation links are active
    - Human approves content tone as "smart friend" not textbook
  </acceptance_criteria>
  <done>Human approves Phase 3 content system. Type "approved" to complete or describe issues.</done>
</task>

</tasks>

<verification>
- Full build pipeline passes (TypeScript + Velite + Next.js)
- All content pages render correctly
- Human approves content quality and visual appearance
</verification>

<success_criteria>
- Zero build errors
- All 10 guide pages, 6 hub pages, and glossary page render correctly
- Human verification confirms content quality, visual design, and interactive features
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-system-life-stage-hubs/03-07-SUMMARY.md`
</output>
